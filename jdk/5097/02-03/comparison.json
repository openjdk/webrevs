{"files":[{"patch":"@@ -365,8 +365,5 @@\n-            long count = fc.size() - pos;\n-            if (pos >= 0 && count >= 0) {\n-                transferred = fc.transferTo(pos, Long.MAX_VALUE, fos.getChannel());\n-                long newPos = pos + transferred;\n-                fc.position(newPos);\n-                if (newPos >= fc.size()) {\n-                    return transferred;\n-                }\n+            transferred = fc.transferTo(pos, Long.MAX_VALUE, fos.getChannel());\n+            long newPos = pos + transferred;\n+            fc.position(newPos);\n+            if (newPos >= fc.size()) {\n+                return transferred;\n","filename":"src\/java.base\/share\/classes\/java\/io\/FileInputStream.java","additions":5,"deletions":8,"binary":false,"changes":13,"status":"modified"}]}