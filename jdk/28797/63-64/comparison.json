{"files":[{"patch":"@@ -61,0 +61,6 @@\n+     * To reduce memory contention, the heap is maintained solely via\n+     * local variables in method loop() (forcing noticeable code\n+     * sprawl), recording only the current heap size when blocked to\n+     * allow method canShutDown to conservatively check emptiness, and\n+     * to report approximate current size for monitoring.\n+     *\n@@ -70,8 +76,0 @@\n-     * When callers submit tasks, they use a form of queue delegation\n-     * locking (see Klaftenegger et al SPAA 2014 and many follow-ups):\n-     * If neither the scheduler nor other submitters are active, they\n-     * acquire lock (via a form of tryLock), update heap, trigger\n-     * ready tasks, and then exit (see method helpProcess). This\n-     * avoids unnecessarily waking up the scheduler, while maintaining\n-     * lock-free progress.\n-     *\n@@ -83,1 +81,1 @@\n-     * status, unparking if necessary. Threads processing  tasks set\n+     * status, unparking if necessary. The scheduler thread sets\n@@ -85,1 +83,1 @@\n-     * rechecks before exiting.  The sstate field takes on a\n+     * rechecks before actually parking.  The active field takes on a\n@@ -129,8 +127,1 @@\n-    ForkJoinPool pool;\n-    ScheduledForkJoinTask<?>[] heapArray;\n-    long nextWakeup;\n-    int heapSize;\n-    volatile int cancelDelayedTasksOnShutdown; \/\/ policy control\n-    boolean removedPeriodicTasks;\n-\n-    @jdk.internal.vm.annotation.Contended(\"p\")\n+    ForkJoinPool pool;               \/\/ read once and detached upon starting\n@@ -138,6 +129,5 @@\n-\n-    @jdk.internal.vm.annotation.Contended(\"s\")\n-    volatile int sstate;                       \/\/ scheduler and lock state\n-    private static final int ACTIVE   = 1;\n-    private static final int LOCKED   = 2;\n-    private static final int STOPPED  = 1 << 31;\n+    volatile int active;             \/\/ 0: inactive, -1: stopped, +1: running\n+    int restingSize;                 \/\/ written only before parking\n+    volatile int cancelDelayedTasksOnShutdown; \/\/ policy control\n+    int pad0, pad1, pad2, pad3, pad4, pad5, pad6, pad7;\n+    int pad8, pad9, padA, padB, padC, padD, padE; \/\/ reduce false sharing\n@@ -151,1 +141,1 @@\n-    private static final long SSTATE;\n+    private static final long ACTIVE;\n@@ -156,1 +146,1 @@\n-        SSTATE = U.objectFieldOffset(klass, \"sstate\");\n+        ACTIVE = U.objectFieldOffset(klass, \"active\");\n@@ -163,2 +153,0 @@\n-\theapArray = new ScheduledForkJoinTask<?>[INITIAL_HEAP_CAPACITY];\n-        nextWakeup = Long.MAX_VALUE;\n@@ -180,3 +168,2 @@\n-        int ss;\n-        if (((ss = sstate) & (ACTIVE|STOPPED)) == 0 &&\n-            (U.getAndBitwiseOrInt(this, SSTATE, ACTIVE) & ACTIVE) == 0)\n+        int state;\n+        if ((state = active) == 0 && U.getAndBitwiseOrInt(this, ACTIVE, 1) == 0)\n@@ -184,55 +171,1 @@\n-        return ss;\n-    }\n-\n-    \/**\n-     * Repeatedly waits for text time trigger, acquires lock, and processes tasks\n-     *\/\n-    private void loop() {\n-        outer: for (long triggerTime = Long.MIN_VALUE;;) {\n-            Thread.interrupted();                 \/\/ clear status\n-            long d = (triggerTime == Long.MAX_VALUE) ? 0L : triggerTime - now();\n-            triggerTime = Long.MIN_VALUE;\n-            if (d >= 0L && pending == null)       \/\/ await timer or signal\n-                U.park(false, d);\n-            for (int ss;;) {                      \/\/ acquire lock\n-                if ((ss = sstate) < 0)\n-                    break outer;\n-                if ((ss & LOCKED) == 0 &&\n-                    U.compareAndSetInt(this, SSTATE, ss, (LOCKED|ACTIVE)))\n-                    break;\n-                U.park(false, 0L);\n-            }\n-            try {\n-                triggerTime = nextWakeup = process();\n-            } finally {\n-                if (U.getAndBitwiseAndInt(this, SSTATE, ~LOCKED) < 0 ||\n-                    triggerTime == Long.MIN_VALUE)\n-                    break;\n-            }\n-        }\n-    }\n-\n-    \/**\n-     * If lock is available, processes pending tasks.\n-     *\/\n-    private void helpProcess() {\n-        int ss;\n-        if (((ss = sstate) & (LOCKED|STOPPED)) == 0 &&\n-            U.compareAndSetInt(this, SSTATE, ss, ss | (LOCKED|ACTIVE))) {\n-            long triggerTime;\n-            boolean stop = true, signal = false;\n-            try {\n-                if ((triggerTime = process()) != Long.MIN_VALUE) {\n-                    if (triggerTime < nextWakeup) {\n-                        nextWakeup = triggerTime;\n-                        signal = true;\n-                    }\n-                    stop = false;\n-                }\n-            } finally {\n-                if (U.getAndBitwiseAndInt(this, SSTATE, ~LOCKED) < 0 || stop)\n-                    terminate();\n-                if (stop | signal)\n-                    U.unpark(this);\n-            }\n-        }\n+        return state;\n@@ -252,1 +185,1 @@\n-            helpProcess();\n+            signal();\n@@ -260,13 +193,4 @@\n-        int ss = sstate;\n-        return (ss < 0 || ((ss & ACTIVE) == 0 && pending == null && heapSize <= 0));\n-    }\n-\n-    \/**\n-     * Sets state to STOPPED and terminate pool if it is shutdown\n-     *\/\n-    private void terminate() {\n-        ForkJoinPool p;\n-        heapSize = 0;\n-        U.getAndBitwiseOrInt(this, SSTATE, STOPPED);\n-        if ((p = pool) != null)\n-            p.tryStopIfShutdown(this);\n+        int state = active;\n+        return (state < 0 ||\n+                ((state == 0 || Thread.currentThread() == this) &&\n+                 pending == null && restingSize <= 0));\n@@ -279,1 +203,1 @@\n-        return heapSize;\n+        return (active < 0) ? 0 : restingSize;\n@@ -294,4 +218,12 @@\n-        try {\n-            loop();\n-        } finally {\n-            terminate();\n+        ForkJoinPool p = pool;\n+        pool = null;   \/\/ detach\n+        if (p == null) \/\/ failed initialization\n+            active = -1;\n+        else {\n+            try {\n+                loop(p);\n+            } finally {\n+                restingSize = 0;\n+                active = -1;\n+                p.tryStopIfShutdown(this);\n+            }\n@@ -302,6 +234,7 @@\n-     * While pending tasks exist or ACTIVE set {\n-     *   Process pending tasks in batches, to add or remove from heap\n-     *   Check for shutdown, either exiting or preparing for shutdown when empty\n-     *   Trigger all enabled tasks by submitting them to pool or run if immediate\n-     * }\n-     * return time of next scheduler wakeup\n+     * After initialization, repeatedly:\n+     * 1. If apparently no work,\n+     *    if active, set tentatively inactive,\n+     *    else park until next trigger time, or indefinitely if none\n+     * 2. Process pending tasks in batches, to add or remove from heap\n+     * 3. Check for shutdown, either exiting or preparing for shutdown when empty\n+     * 4. Trigger all enabled tasks by submitting them to pool or run if immediate\n@@ -309,31 +242,11 @@\n-    final long process() {\n-        ForkJoinPool p = pool;\n-        ScheduledForkJoinTask<?>[] h = heapArray;\n-        int n = heapSize, cap;\n-        if (p == null || h == null || n < 0 || (cap = h.length) <= 0)\n-            return Long.MIN_VALUE;            \/\/ only possible after stopping\n-        for (long triggerTime = Long.MAX_VALUE;;) {\n-            ScheduledForkJoinTask<?> q, t; int ss, runStatus;\n-            if ((q = pending) == null) {\n-                heapSize = n;\n-                if (((ss = sstate) & ACTIVE) == 0)\n-                    return triggerTime;\n-                U.compareAndSetInt(this, SSTATE, ss, ss & ~ACTIVE);\n-                q = pending;\n-            }\n-\n-            while (q != null &&               \/\/ process pending tasks\n-                   (t = (ScheduledForkJoinTask<?>)\n-                    U.getAndSetReference(this, PENDING, null)) != null) {\n-                ScheduledForkJoinTask<?> next;\n-                do {\n-                    int i;\n-                    if ((next = t.nextPending) != null)\n-                        t.nextPending = null;\n-                    if ((i = t.heapIndex) >= 0) {\n-                        t.heapIndex = -1;      \/\/ remove cancelled task\n-                        if (i < cap && h[i] == t)\n-                            n = replace(h, i, n);\n-                    }\n-                    else if (n >= cap || n < 0)\n-                        t.trySetCancelled();   \/\/ couldn't resize\n+    private void loop(ForkJoinPool p) {\n+        if (p != null) {                           \/\/ currently always true\n+            ScheduledForkJoinTask<?>[] h =         \/\/ heap array\n+                new ScheduledForkJoinTask<?>[INITIAL_HEAP_CAPACITY];\n+            int cap = h.length, n = 0, prevRunStatus = 0; \/\/ n is heap size\n+            long parkTime = 0L;                    \/\/ zero for untimed park\n+            for (;;) {                             \/\/ loop until stopped\n+                ScheduledForkJoinTask<?> q, t; int runStatus;\n+                if ((q = pending) == null) {\n+                    if (active != 0)               \/\/ deactivate and recheck\n+                        U.compareAndSetInt(this, ACTIVE, 1, 0);\n@@ -341,18 +254,32 @@\n-                        long d = t.when;       \/\/ add and sift up\n-                        if (t.status >= 0) {\n-                            ScheduledForkJoinTask<?> parent;\n-                            int k = n++, pk, newCap;\n-                            while (k > 0 &&\n-                                   (parent = h[pk = (k - 1) >>> 2]) != null &&\n-                                   (parent.when > d)) {\n-                                parent.heapIndex = k;\n-                                h[k] = parent;\n-                                k = pk;\n-                            }\n-                            t.heapIndex = k;\n-                            h[k] = t;\n-                            if (n >= cap && (newCap = cap << 1) > cap) {\n-                                ScheduledForkJoinTask<?>[] a = null;\n-                                try {          \/\/ try to resize\n-                                    a = Arrays.copyOf(h, newCap);\n-                                } catch (Error | RuntimeException ex) {\n+                        Thread.interrupted();      \/\/ clear before park\n+                        U.park(false, parkTime);\n+                    }\n+                    q = pending;\n+                }\n+\n+                while (q != null &&                \/\/ process pending tasks\n+                       (t = (ScheduledForkJoinTask<?>)\n+                        U.getAndSetReference(this, PENDING, null)) != null) {\n+                    ScheduledForkJoinTask<?> next;\n+                    do {\n+                        int i;\n+                        if ((next = t.nextPending) != null)\n+                            t.nextPending = null;\n+                        if ((i = t.heapIndex) >= 0) {\n+                            t.heapIndex = -1;      \/\/ remove cancelled task\n+                            if (i < cap && h[i] == t)\n+                                n = replace(h, i, n);\n+                        }\n+                        else if (n >= cap || n < 0)\n+                            t.trySetCancelled();   \/\/ couldn't resize\n+                        else {\n+                            long d = t.when;       \/\/ add and sift up\n+                            if (t.status >= 0) {\n+                                ScheduledForkJoinTask<?> parent;\n+                                int k = n++, pk, newCap;\n+                                while (k > 0 &&\n+                                       (parent = h[pk = (k - 1) >>> 2]) != null &&\n+                                       (parent.when > d)) {\n+                                    parent.heapIndex = k;\n+                                    h[k] = parent;\n+                                    k = pk;\n@@ -360,3 +287,12 @@\n-                                if (a != null && a.length == newCap) {\n-                                    cap = newCap; \/\/ else keep using old array\n-                                    h = heapArray = a;\n+                                t.heapIndex = k;\n+                                h[k] = t;\n+                                if (n >= cap && (newCap = cap << 1) > cap) {\n+                                    ScheduledForkJoinTask<?>[] a = null;\n+                                    try {          \/\/ try to resize\n+                                        a = Arrays.copyOf(h, newCap);\n+                                    } catch (Error | RuntimeException ex) {\n+                                    }\n+                                    if (a != null && a.length == newCap) {\n+                                        cap = newCap;\n+                                        h = a;     \/\/ else keep using old array\n+                                    }\n@@ -366,4 +302,3 @@\n-                    }\n-                } while ((t = next) != null);\n-                q = pending;\n-            }\n+                    } while ((t = next) != null);\n+                    q = pending;\n+                }\n@@ -371,3 +306,5 @@\n-            if ((runStatus = p.shutdownStatus(this)) != 0 &&\n-                (n = tryStop(p, h, n, runStatus)) < 0)\n-                return Long.MIN_VALUE;\n+                if ((runStatus = p.shutdownStatus(this)) != 0) {\n+                    if ((n = tryStop(p, h, n, runStatus, prevRunStatus)) < 0)\n+                        break;\n+                    prevRunStatus = runStatus;\n+                }\n@@ -375,17 +312,10 @@\n-            triggerTime = Long.MAX_VALUE;\n-            if (n > 0 && h.length > 0) {    \/\/ submit enabled tasks\n-                long now = now();\n-                do {\n-                    ScheduledForkJoinTask<?> f; int stat;\n-                    if ((f = h[0]) != null) {\n-                        long d = (triggerTime = f.when) - now;\n-                        if ((stat = f.status) >= 0 && d > 0L)\n-                            break;\n-                        f.heapIndex = -1;\n-                        if (stat < 0)\n-                            ;               \/\/ already cancelled\n-                        else if (f.isImmediate)\n-                            f.doExec();\n-                        else {\n-                            try {\n-                                p.executeEnabledScheduledTask(f);\n+                parkTime = 0L;\n+                if (n > 0 && h.length > 0) {    \/\/ submit enabled tasks\n+                    long now = now();\n+                    do {\n+                        ScheduledForkJoinTask<?> f; int stat;\n+                        if ((f = h[0]) != null) {\n+                            long d = f.when - now;\n+                            if ((stat = f.status) >= 0 && d > 0L) {\n+                                parkTime = d;\n+                                break;\n@@ -393,2 +323,12 @@\n-                            catch (Error | RuntimeException ex) {\n-                                f.trySetCancelled();\n+                            f.heapIndex = -1;\n+                            if (stat < 0)\n+                                ;               \/\/ already cancelled\n+                            else if (f.isImmediate)\n+                                f.doExec();\n+                            else {\n+                                try {\n+                                    p.executeEnabledScheduledTask(f);\n+                                }\n+                                catch (Error | RuntimeException ex) {\n+                                    f.trySetCancelled();\n+                                }\n@@ -397,3 +337,3 @@\n-                    }\n-                    triggerTime = Long.MAX_VALUE;\n-                } while ((n = heapSize = replace(h, 0, n)) > 0);\n+                    } while ((n = replace(h, 0, n)) > 0);\n+                }\n+                restingSize = n;\n@@ -478,1 +418,1 @@\n-                        int runStatus) {\n+                        int runStatus, int prevRunStatus) {\n@@ -482,1 +422,0 @@\n-                    removedPeriodicTasks = true;\n@@ -486,3 +425,2 @@\n-                else if (!removedPeriodicTasks && h != null && h.length >= n) {\n-                    ScheduledForkJoinTask<?> t; int stat;\n-                    removedPeriodicTasks = true;\n+                else if (prevRunStatus == 0 && h != null && h.length >= n) {\n+                    ScheduledForkJoinTask<?> t; int stat; \/\/ remove periodic tasks\n@@ -500,2 +438,3 @@\n-            if (n > 0 || p == null || !p.tryStopIfShutdown(this))\n-                return heapSize = n;  \/\/ check for quiescent shutdown\n+            if ((restingSize = n) > 0 || pending != null ||\n+                p == null || !p.tryStopIfShutdown(this))\n+                return n;       \/\/ check for quiescent shutdown\n@@ -503,1 +442,1 @@\n-        heapSize = 0;\n+        restingSize = 0;\n","filename":"src\/java.base\/share\/classes\/java\/util\/concurrent\/DelayScheduler.java","additions":139,"deletions":200,"binary":false,"changes":339,"status":"modified"},{"patch":"@@ -1291,1 +1291,1 @@\n-                else if (((room & m) == 0 ||\n+                if ((room == 0 || room == m ||\n@@ -2817,3 +2817,3 @@\n-                if (ctl != 0L) \/\/ else loop if didn't finish cleaning\n-                    break;\n-                if ((ds = delayScheduler) != null && ds.signal() >= 0)\n+                if (((ds = delayScheduler) != null && ds.signal() >= 0) ||\n+                    ctl != 0L) { \/\/ else loop if didn't finish cleaning\n+                    releaseWaiters();\n@@ -2821,0 +2821,1 @@\n+                }\n@@ -2894,0 +2895,7 @@\n+        DelayScheduler ds;\n+        if ((ds = delayScheduler) != null) {\n+            try {\n+                ds.interrupt();\n+            } catch (Throwable ignore) {\n+            }\n+        }\n","filename":"src\/java.base\/share\/classes\/java\/util\/concurrent\/ForkJoinPool.java","additions":12,"deletions":4,"binary":false,"changes":16,"status":"modified"}]}