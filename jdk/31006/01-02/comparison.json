{"files":[{"patch":"@@ -696,6 +696,16 @@\n-        return regionToSkeletonMap != null ?\n-            regionToSkeletonMap.getOrDefault(locale.getLanguage() + \"-\" + locale.getCountry(),\n-                regionToSkeletonMap.getOrDefault(locale.getCountry(),\n-                    regionToSkeletonMap.getOrDefault(locale.getLanguage() + \"-001\",\n-                        regionToSkeletonMap.getOrDefault(\"001\", \"h\")))) :\n-            \"h\";\n+        var hour = \"h\";\n+\n+        if (regionToSkeletonMap != null) {\n+            for (var region: new String[] {\n+                locale.getLanguage() + \"-\" + locale.getCountry(),\n+                locale.getCountry(),\n+                locale.getLanguage() + \"-001\",\n+                \"001\"}) {\n+                hour = regionToSkeletonMap.get(region);\n+                if (hour != null) {\n+                    break;\n+                }\n+            }\n+        }\n+\n+        return hour;\n","filename":"src\/java.base\/share\/classes\/sun\/util\/locale\/provider\/LocaleResources.java","additions":16,"deletions":6,"binary":false,"changes":22,"status":"modified"},{"patch":"@@ -50,1 +50,3 @@\n-        assertDoesNotThrow(this::doRaceTest);\n+        for (int run = 0; run < 10; run++) {\n+            assertDoesNotThrow(this::doRaceTest);\n+        }\n@@ -56,4 +58,0 @@\n-        ExecutorService pool = Executors.newFixedThreadPool(threads);\n-        CountDownLatch ready = new CountDownLatch(threads);\n-        CountDownLatch go = new CountDownLatch(1);\n-        List<Future<?>> futures = new ArrayList<>();\n@@ -61,10 +59,4 @@\n-        for (int i = 0; i < threads; i++) {\n-            Locale locale = locales[i % locales.length];\n-            futures.add(pool.submit(() -> {\n-                ready.countDown();\n-                go.await();\n-                DateTimeFormatterBuilder.getLocalizedDateTimePattern(\n-                    \"yMd\", IsoChronology.INSTANCE, locale);\n-                return null;\n-            }));\n-        }\n+        try (ExecutorService pool = Executors.newVirtualThreadPerTaskExecutor()) {\n+            CountDownLatch ready = new CountDownLatch(threads);\n+            CountDownLatch go = new CountDownLatch(1);\n+            List<Future<?>> futures = new ArrayList<>();\n@@ -72,4 +64,15 @@\n-        ready.await();\n-        go.countDown();\n-        pool.shutdown();\n-        for (Future<?> f : futures) f.get();\n+            for (int i = 0; i < threads; i++) {\n+                Locale locale = locales[i % locales.length];\n+                futures.add(pool.submit(() -> {\n+                    ready.countDown();\n+                    go.await();\n+                    DateTimeFormatterBuilder.getLocalizedDateTimePattern(\n+                        \"yMd\", IsoChronology.INSTANCE, locale);\n+                    return null;\n+                }));\n+            }\n+\n+            ready.await();\n+            go.countDown();\n+            for (Future<?> f : futures) f.get();\n+        }\n","filename":"test\/jdk\/sun\/util\/locale\/provider\/SkeletonRaceTest.java","additions":22,"deletions":19,"binary":false,"changes":41,"status":"modified"}]}