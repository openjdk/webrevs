{"files":[{"patch":"@@ -2658,4 +2658,5 @@\n-\/\/ Returns true if (n, m) is the (XorVMask, MaskAll) part of an \"andNot\"\n-\/\/ pattern: n is XorVMask, m is all-ones, and n is used only by an AndVMask.\n-\/\/ That gives (AndVMask vm1 (XorVMask vm2 (MaskAll m1))). Cloning m allows\n-\/\/ the matcher to share the MaskAll and match the andNot rule.\n+\/\/ Returns true if (n, m) matches \"(XorVMask vm2 (MaskAll m1))\" and that XorVMask\n+\/\/ is used only by an AndVMask. In that case, cloning m (the MaskAll) lets the\n+\/\/ matcher avoid sharing the MaskAll node and subsume the pattern into rule:\n+\/\/   \"(AndVMask vm1 (XorVMask vm2 (MaskAll m1)))\".\n+\/\/\n@@ -2664,1 +2665,1 @@\n-static bool is_vector_mask_bitwise_andnot_pattern(Node* n, Node* m) {\n+static bool is_vector_mask_not_operand_in_andnot_pattern(Node* n, Node* m) {\n@@ -2669,9 +2670,10 @@\n-  if (n->Opcode() == Op_XorVMask &&\n-      VectorNode::is_all_ones_vector(m)) {\n-    \/\/ Check whether n is only used by an AndVMask node.\n-    if (n->outcnt() == 1) {\n-      Node* u = n->unique_out();\n-      if (u->Opcode() == Op_AndVMask) {\n-        return true;\n-      }\n-    }\n+  if (VectorNode::is_all_ones_vector(m) &&\n+      n->Opcode() == Op_XorVMask &&\n+      n->outcnt() == 1 &&\n+      n->unique_out()->Opcode() == Op_AndVMask) {\n+    \/\/ If another input of the AndVMask is also a mask-not pattern that would\n+    \/\/ qualify for the `maskAll` cloning, do not clone the \"maskAll\" here,\n+    \/\/ because the match rule can only consume one such pattern.\n+    Node* use = n->unique_out();\n+    Node* other_input = use->in(1) == n ? use->in(2) : use->in(1);\n+    return !VectorNode::is_vectormask_bitwise_not_pattern(other_input);\n@@ -2686,1 +2688,1 @@\n-      is_vector_mask_bitwise_andnot_pattern(n, m) ||\n+      is_vector_mask_not_operand_in_andnot_pattern(n, m) ||\n","filename":"src\/hotspot\/cpu\/aarch64\/aarch64.ad","additions":17,"deletions":15,"binary":false,"changes":32,"status":"modified"},{"patch":"@@ -879,0 +879,7 @@\n+bool VectorNode::is_vectormask_bitwise_not_pattern(Node* n) {\n+  if (n->Opcode() == Op_XorVMask) {\n+    return is_all_ones_vector(n->in(1)) ||\n+           is_all_ones_vector(n->in(2));\n+  }\n+  return false;\n+}\n","filename":"src\/hotspot\/share\/opto\/vectornode.cpp","additions":7,"deletions":0,"binary":false,"changes":7,"status":"modified"},{"patch":"@@ -176,0 +176,1 @@\n+  static bool is_vectormask_bitwise_not_pattern(Node* n);\n","filename":"src\/hotspot\/share\/opto\/vectornode.hpp","additions":1,"deletions":0,"binary":false,"changes":1,"status":"modified"},{"patch":"@@ -42,1 +42,1 @@\n- * @bug 8287984\n+ * @bug 8287984 8378737\n@@ -328,0 +328,30 @@\n+    \/\/ Tests that mask.not().andNot(other) doesn't match to VMASK_AND_NOT rule on SVE,\n+    \/\/ and only one MaskAll machine node is generated.\n+    @Test\n+    @IR(counts = { IRNode.AARCH64_VMASK_ALL_IMM_I, \"1\", IRNode.VMASK_AND_NOT_I, \"0\" },\n+        applyIfCPUFeature = {\"sve\", \"true\"})\n+    public static void testSingleMaskAllWithAndNotI() {\n+        VectorMask<Integer> avm = VectorMask.fromArray(I_SPECIES, ma, 0);\n+        VectorMask<Integer> bvm = VectorMask.fromArray(I_SPECIES, mb, 0);\n+        avm.not().andNot(bvm).intoArray(mr, 0);\n+\n+        \/\/ Verify results\n+        for (int i = 0; i < I_SPECIES.length(); i++) {\n+            Asserts.assertEquals((!ma[i]) & (!mb[i]), mr[i]);\n+        }\n+    }\n+\n+    @Test\n+    @IR(counts = { IRNode.AARCH64_VMASK_ALL_IMM_L, \"1\", IRNode.VMASK_AND_NOT_L, \"0\" },\n+        applyIfCPUFeature = {\"sve\", \"true\"})\n+    public static void testSingleMaskAllWithAndNotL() {\n+        VectorMask<Long> avm = VectorMask.fromArray(L_SPECIES, ma, 0);\n+        VectorMask<Long> bvm = VectorMask.fromArray(L_SPECIES, mb, 0);\n+        avm.not().andNot(bvm).intoArray(mr, 0);\n+\n+        \/\/ Verify results\n+        for (int i = 0; i < L_SPECIES.length(); i++) {\n+            Asserts.assertEquals((!ma[i]) & (!mb[i]), mr[i]);\n+        }\n+    }\n+\n","filename":"test\/hotspot\/jtreg\/compiler\/vectorapi\/AllBitsSetVectorMatchRuleTest.java","additions":31,"deletions":1,"binary":false,"changes":32,"status":"modified"}]}