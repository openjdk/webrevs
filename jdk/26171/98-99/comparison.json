{"files":[{"patch":"@@ -314,1 +314,2 @@\n-  size_t available = _space_info->available();\n+  size_t available = _space_info->available()\n+                   + ShenandoahHeap::heap()->free_set()->mutator_allocator()->remaining_bytes();\n","filename":"src\/hotspot\/share\/gc\/shenandoah\/heuristics\/shenandoahAdaptiveHeuristics.cpp","additions":2,"deletions":1,"binary":false,"changes":3,"status":"modified"},{"patch":"@@ -412,4 +412,7 @@\n-  \/\/ Heap lock guards the slot pointers; load_relaxed is sufficient under it.\n-  \/\/ r->free() may still observe a concurrently advancing _atomic_top, which\n-  \/\/ is fine for heuristic callers.\n-  shenandoah_assert_heaplocked();\n+  \/\/ Best-effort snapshot of bytes still available in the allocator's reserved\n+  \/\/ regions. Callers may invoke this without the heap lock (e.g. via\n+  \/\/ unsafe_max_tlab_alloc on the TLAB fast path). Slot pointers are read with\n+  \/\/ acquire ordering; r->free() tolerates concurrent CAS allocation via the\n+  \/\/ concurrent top() accessor. The returned value is a point-in-time snapshot;\n+  \/\/ do not compose with other separately-sampled allocator values without\n+  \/\/ considering that mutators may CAS-allocate between samples.\n@@ -418,1 +421,1 @@\n-    ShenandoahHeapRegion* r = _alloc_regions[i].address.load_relaxed();\n+    ShenandoahHeapRegion* r = _alloc_regions[i].address.load_acquire();\n","filename":"src\/hotspot\/share\/gc\/shenandoah\/shenandoahAllocator.cpp","additions":8,"deletions":5,"binary":false,"changes":13,"status":"modified"},{"patch":"@@ -78,1 +78,15 @@\n-  return MIN2(ShenandoahHeapRegion::max_tlab_size_bytes(), young_generation()->available());\n+  const size_t max_tlab = ShenandoahHeapRegion::max_tlab_size_bytes();\n+  const size_t gen_available = young_generation()->available();\n+  \/\/ Fast path: if the generation's partition-view available is at least a\n+  \/\/ full max TLAB, the MIN2 result is max_tlab regardless of the bytes still\n+  \/\/ held inside the mutator allocator's active CAS alloc regions. Skip\n+  \/\/ summing the allocator's per-slot free bytes, which is O(N) over the\n+  \/\/ number of alloc-region slots and is called on every TLAB refill.\n+  if (gen_available >= max_tlab) {\n+    return max_tlab;\n+  }\n+  \/\/ Slow path: the partition view alone is not enough. Account for the bytes\n+  \/\/ still free inside the mutator allocator's active regions, which are\n+  \/\/ retired from the Mutator partition at reservation time and therefore\n+  \/\/ not reflected in young_generation()->available().\n+  return MIN2(max_tlab, gen_available + free_set()->mutator_allocator()->remaining_bytes());\n","filename":"src\/hotspot\/share\/gc\/shenandoah\/shenandoahGenerationalHeap.cpp","additions":15,"deletions":1,"binary":false,"changes":16,"status":"modified"}]}