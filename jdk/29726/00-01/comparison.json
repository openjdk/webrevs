{"files":[{"patch":"@@ -177,1 +177,0 @@\n-        long glyphPtr = getGlyphImagePtr(glyphCode);\n@@ -179,7 +178,10 @@\n-        metrics.x = StrikeCache.getGlyphXAdvance(glyphPtr);\n-        metrics.y = StrikeCache.getGlyphYAdvance(glyphPtr);\n-        \/* advance is currently in device space, need to convert back\n-         * into user space.\n-         * This must not include the translation component. *\/\n-        if (invDevTx != null) {\n-            invDevTx.deltaTransform(metrics, metrics);\n+        long glyphPtr = getGlyphImagePtr(glyphCode);\n+        if (glyphPtr != 0L) {\n+            metrics.x = StrikeCache.getGlyphXAdvance(glyphPtr);\n+            metrics.y = StrikeCache.getGlyphYAdvance(glyphPtr);\n+            \/* advance is currently in device space, need to convert back\n+             * into user space.\n+             * This must not include the translation component. *\/\n+            if (invDevTx != null) {\n+                invDevTx.deltaTransform(metrics, metrics);\n+            }\n","filename":"src\/java.desktop\/macosx\/classes\/sun\/font\/CStrike.java","additions":10,"deletions":8,"binary":false,"changes":18,"status":"modified"}]}