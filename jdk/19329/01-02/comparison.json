{"files":[{"patch":"@@ -562,3 +562,10 @@\n-    if (sp.dirlen > 0 && c->pdir != NULL) {\n-        memcpy(buf+offset, c->pdir, sp.dirlen);\n-        offset += sp.dirlen;\n+    if (c->pdir != NULL) {\n+        if (sp.dirlen > 0) {\n+            memcpy(buf+offset, c->pdir, sp.dirlen);\n+            offset += sp.dirlen;\n+        }\n+    } else {\n+        if (sp.dirlen > 0) {\n+            free(buf);\n+            return -1;\n+        }\n","filename":"src\/java.base\/unix\/native\/libjava\/ProcessImpl_md.c","additions":10,"deletions":3,"binary":false,"changes":13,"status":"modified"}]}