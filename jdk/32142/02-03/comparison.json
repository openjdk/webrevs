{"files":[{"patch":"@@ -69,1 +69,0 @@\n- *      -jdb.option=-trackallthreads\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/exclude\/exclude001\/exclude001.java","additions":0,"deletions":1,"binary":false,"changes":1,"status":"modified"},{"patch":"@@ -57,1 +57,0 @@\n- *      -jdb.option=-trackallthreads\n@@ -87,0 +86,1 @@\n+    static final String THREAD_STARTED_BREAK = PACKAGE_NAME + \".interrupt001a$MyThread.threadStarted\";\n@@ -109,1 +109,33 @@\n-        reply = jdb.receiveReplyFor(JdbCommand.cont);\n+        jdb.setBreakpointInMethod(THREAD_STARTED_BREAK);\n+\n+        \/\/ Continue until the debuggee reaches the LAST_BREAK breakpoint. Each\n+        \/\/ tested thread first hits threadStarted(); receiving that event is\n+        \/\/ what makes a virtual thread visible to jdb with the default debug\n+        \/\/ agent behavior, so count the hits to be sure every tested thread\n+        \/\/ became visible before the lookup below.\n+        int started = 0;\n+        while (true) {\n+            String[] contReply = jdb.receiveReplyFor(JdbCommand.cont);\n+            \/\/ Match the fully qualified method of the stop location as a single\n+            \/\/ token: debuggee output can interleave with jdb's own output and\n+            \/\/ split the \"Breakpoint hit:\" prefix away from the location, so do\n+            \/\/ not require both in the same paragraph. The \"(\" suffix avoids\n+            \/\/ matching jdb's \"Set deferred breakpoint\" messages.\n+            Paragrep contGrep = new Paragrep(contReply);\n+            if (contGrep.find(LAST_BREAK + \"(\") > 0) {\n+                break;\n+            }\n+            if (contGrep.find(THREAD_STARTED_BREAK + \"(\") > 0) {\n+                started++;\n+                continue;\n+            }\n+            failure(\"Stopped at unexpected location, expected \" + LAST_BREAK\n+                    + \" or \" + THREAD_STARTED_BREAK);\n+            for (String line : contReply) {\n+                log.complain(\"reply: \" + line);\n+            }\n+            break;\n+        }\n+        if (started != numThreads) {\n+            failure(\"Expected \" + numThreads + \" threadStarted() hits, got: \" + started);\n+        }\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/interrupt\/interrupt001\/interrupt001.java","additions":34,"deletions":2,"binary":false,"changes":36,"status":"modified"},{"patch":"@@ -48,0 +48,6 @@\n+        \/\/ Each tested thread calls this once at startup. The debugger sets a\n+        \/\/ breakpoint here: receiving the event is what makes a virtual thread\n+        \/\/ visible to jdb with the default debug agent behavior, so the test\n+        \/\/ does not need the -trackallthreads option.\n+        static void threadStarted() {}\n+\n@@ -49,0 +55,1 @@\n+            threadStarted();\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/interrupt\/interrupt001\/interrupt001a.java","additions":7,"deletions":0,"binary":false,"changes":7,"status":"modified"},{"patch":"@@ -57,1 +57,0 @@\n- *      -jdb.option=-trackallthreads\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/pop\/pop001\/pop001.java","additions":0,"deletions":1,"binary":false,"changes":1,"status":"modified"},{"patch":"@@ -62,1 +62,0 @@\n- *      -jdb.option=-trackallthreads\n@@ -91,0 +90,1 @@\n+    static final String THREAD_STARTED_BREAK = PACKAGE_NAME + \".read001aTestedThread.threadStarted\";\n@@ -101,1 +101,33 @@\n-        reply = jdb.receiveReplyFor(JdbCommand.cont);\n+        jdb.setBreakpointInMethod(THREAD_STARTED_BREAK);\n+\n+        \/\/ Continue until the debuggee reaches the LAST_BREAK breakpoint. Each\n+        \/\/ tested thread first hits threadStarted(); receiving that event is\n+        \/\/ what makes a virtual thread visible to jdb with the default debug\n+        \/\/ agent behavior, so count the hits to be sure every tested thread\n+        \/\/ became visible before the lookup below.\n+        int started = 0;\n+        while (true) {\n+            String[] contReply = jdb.receiveReplyFor(JdbCommand.cont);\n+            \/\/ Match the fully qualified method of the stop location as a single\n+            \/\/ token: debuggee output can interleave with jdb's own output and\n+            \/\/ split the \"Breakpoint hit:\" prefix away from the location, so do\n+            \/\/ not require both in the same paragraph. The \"(\" suffix avoids\n+            \/\/ matching jdb's \"Set deferred breakpoint\" messages.\n+            Paragrep contGrep = new Paragrep(contReply);\n+            if (contGrep.find(LAST_BREAK + \"(\") > 0) {\n+                break;\n+            }\n+            if (contGrep.find(THREAD_STARTED_BREAK + \"(\") > 0) {\n+                started++;\n+                continue;\n+            }\n+            failure(\"Stopped at unexpected location, expected \" + LAST_BREAK\n+                    + \" or \" + THREAD_STARTED_BREAK);\n+            for (String line : contReply) {\n+                log.complain(\"reply: \" + line);\n+            }\n+            break;\n+        }\n+        if (started != 1) {\n+            failure(\"Expected \" + 1 + \" threadStarted() hits, got: \" + started);\n+        }\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/read\/read001\/read001.java","additions":34,"deletions":2,"binary":false,"changes":36,"status":"modified"},{"patch":"@@ -100,0 +100,6 @@\n+    \/\/ Each tested thread calls this once at startup. The debugger sets a\n+    \/\/ breakpoint here: receiving the event is what makes a virtual thread\n+    \/\/ visible to jdb with the default debug agent behavior, so the test\n+    \/\/ does not need the -trackallthreads option.\n+    static void threadStarted() {}\n+\n@@ -101,0 +107,1 @@\n+        threadStarted();\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/read\/read001\/read001a.java","additions":7,"deletions":0,"binary":false,"changes":7,"status":"modified"},{"patch":"@@ -56,1 +56,0 @@\n- *      -jdb.option=-trackallthreads\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/reenter\/reenter001\/reenter001.java","additions":0,"deletions":1,"binary":false,"changes":1,"status":"modified"},{"patch":"@@ -60,1 +60,0 @@\n- *      -jdb.option=-trackallthreads\n@@ -88,0 +87,1 @@\n+    static final String THREAD_STARTED_BREAK = PACKAGE_NAME + \".MyThread.threadStarted\";\n@@ -99,1 +99,33 @@\n-        jdb.receiveReplyFor(JdbCommand.cont);\n+        jdb.setBreakpointInMethod(THREAD_STARTED_BREAK);\n+\n+        \/\/ Continue until the debuggee reaches the LAST_BREAK breakpoint. Each\n+        \/\/ tested thread first hits threadStarted(); receiving that event is\n+        \/\/ what makes a virtual thread visible to jdb with the default debug\n+        \/\/ agent behavior, so count the hits to be sure every tested thread\n+        \/\/ became visible before the lookup below.\n+        int started = 0;\n+        while (true) {\n+            String[] contReply = jdb.receiveReplyFor(JdbCommand.cont);\n+            \/\/ Match the fully qualified method of the stop location as a single\n+            \/\/ token: debuggee output can interleave with jdb's own output and\n+            \/\/ split the \"Breakpoint hit:\" prefix away from the location, so do\n+            \/\/ not require both in the same paragraph. The \"(\" suffix avoids\n+            \/\/ matching jdb's \"Set deferred breakpoint\" messages.\n+            Paragrep contGrep = new Paragrep(contReply);\n+            if (contGrep.find(LAST_BREAK + \"(\") > 0) {\n+                break;\n+            }\n+            if (contGrep.find(THREAD_STARTED_BREAK + \"(\") > 0) {\n+                started++;\n+                continue;\n+            }\n+            failure(\"Stopped at unexpected location, expected \" + LAST_BREAK\n+                    + \" or \" + THREAD_STARTED_BREAK);\n+            for (String line : contReply) {\n+                log.complain(\"reply: \" + line);\n+            }\n+            break;\n+        }\n+        if (started != resume002a.numThreads) {\n+            failure(\"Expected \" + resume002a.numThreads + \" threadStarted() hits, got: \" + started);\n+        }\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/resume\/resume002\/resume002.java","additions":34,"deletions":2,"binary":false,"changes":36,"status":"modified"},{"patch":"@@ -114,0 +114,6 @@\n+    \/\/ Each tested thread calls this once at startup. The debugger sets a\n+    \/\/ breakpoint here: receiving the event is what makes a virtual thread\n+    \/\/ visible to jdb with the default debug agent behavior, so the test\n+    \/\/ does not need the -trackallthreads option.\n+    static void threadStarted() {}\n+\n@@ -115,0 +121,1 @@\n+        threadStarted();\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/resume\/resume002\/resume002a.java","additions":7,"deletions":0,"binary":false,"changes":7,"status":"modified"},{"patch":"@@ -52,1 +52,0 @@\n- *      -jdb.option=-trackallthreads\n@@ -80,0 +79,1 @@\n+    static final String THREAD_STARTED_BREAK = PACKAGE_NAME + \".MyThread.threadStarted\";\n@@ -86,1 +86,34 @@\n-        jdb.receiveReplyFor(JdbCommand.cont);\n+        jdb.setBreakpointInMethod(THREAD_STARTED_BREAK);\n+\n+        \/\/ Continue until the main thread reaches lastBreak(). Each tested\n+        \/\/ thread first hits threadStarted(); receiving that event is what\n+        \/\/ makes a virtual thread visible to jdb with the default debug agent\n+        \/\/ behavior, so count the hits to be sure every tested thread became\n+        \/\/ visible before the lookup below.\n+        int started = 0;\n+        while (true) {\n+            String[] contReply = jdb.receiveReplyFor(JdbCommand.cont);\n+            \/\/ Match the fully qualified method of the stop location as a single\n+            \/\/ token: debuggee output can interleave with jdb's own output and\n+            \/\/ split the \"Breakpoint hit:\" prefix away from the location, so do\n+            \/\/ not require both in the same paragraph. The \"(\" suffix avoids\n+            \/\/ matching jdb's \"Set deferred breakpoint\" messages.\n+            Paragrep contGrep = new Paragrep(contReply);\n+            if (contGrep.find(LAST_BREAK + \"(\") > 0) {\n+                break;\n+            }\n+            if (contGrep.find(THREAD_STARTED_BREAK + \"(\") > 0) {\n+                started++;\n+                continue;\n+            }\n+            failure(\"Stopped at unexpected location, expected \" + LAST_BREAK\n+                    + \" or \" + THREAD_STARTED_BREAK);\n+            for (String line : contReply) {\n+                log.complain(\"reply: \" + line);\n+            }\n+            break;\n+        }\n+        if (started != thread002a.numThreads) {\n+            failure(\"Expected \" + thread002a.numThreads\n+                    + \" threadStarted() hits, got: \" + started);\n+        }\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/thread\/thread002\/thread002.java","additions":35,"deletions":2,"binary":false,"changes":37,"status":"modified"},{"patch":"@@ -114,0 +114,6 @@\n+    \/\/ Each tested thread calls this once at startup. The debugger sets a\n+    \/\/ breakpoint here: receiving the event is what makes a virtual thread\n+    \/\/ visible to jdb with the default debug agent behavior, so the test\n+    \/\/ does not need the -trackallthreads option.\n+    static void threadStarted() {}\n+\n@@ -115,0 +121,1 @@\n+        threadStarted();\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/thread\/thread002\/thread002a.java","additions":7,"deletions":0,"binary":false,"changes":7,"status":"modified"},{"patch":"@@ -64,1 +64,0 @@\n- *      -jdb.option=-trackallthreads\n@@ -92,0 +91,1 @@\n+    static final String THREAD_STARTED_BREAK = PACKAGE_NAME + \".MyThread.threadStarted\";\n@@ -106,1 +106,33 @@\n-        reply = jdb.receiveReplyFor(JdbCommand.cont);\n+        jdb.setBreakpointInMethod(THREAD_STARTED_BREAK);\n+\n+        \/\/ Continue until the debuggee reaches the LAST_BREAK breakpoint. Each\n+        \/\/ tested thread first hits threadStarted(); receiving that event is\n+        \/\/ what makes a virtual thread visible to jdb with the default debug\n+        \/\/ agent behavior, so count the hits to be sure every tested thread\n+        \/\/ became visible before the lookup below.\n+        int started = 0;\n+        while (true) {\n+            String[] contReply = jdb.receiveReplyFor(JdbCommand.cont);\n+            \/\/ Match the fully qualified method of the stop location as a single\n+            \/\/ token: debuggee output can interleave with jdb's own output and\n+            \/\/ split the \"Breakpoint hit:\" prefix away from the location, so do\n+            \/\/ not require both in the same paragraph. The \"(\" suffix avoids\n+            \/\/ matching jdb's \"Set deferred breakpoint\" messages.\n+            Paragrep contGrep = new Paragrep(contReply);\n+            if (contGrep.find(LAST_BREAK + \"(\") > 0) {\n+                break;\n+            }\n+            if (contGrep.find(THREAD_STARTED_BREAK + \"(\") > 0) {\n+                started++;\n+                continue;\n+            }\n+            failure(\"Stopped at unexpected location, expected \" + LAST_BREAK\n+                    + \" or \" + THREAD_STARTED_BREAK);\n+            for (String line : contReply) {\n+                log.complain(\"reply: \" + line);\n+            }\n+            break;\n+        }\n+        if (started != untrace001a.numThreads) {\n+            failure(\"Expected \" + untrace001a.numThreads + \" threadStarted() hits, got: \" + started);\n+        }\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/untrace\/untrace001\/untrace001.java","additions":34,"deletions":2,"binary":false,"changes":36,"status":"modified"},{"patch":"@@ -142,0 +142,6 @@\n+    \/\/ Each tested thread calls this once at startup. The debugger sets a\n+    \/\/ breakpoint here: receiving the event is what makes a virtual thread\n+    \/\/ visible to jdb with the default debug agent behavior, so the test\n+    \/\/ does not need the -trackallthreads option.\n+    static void threadStarted() {}\n+\n@@ -143,0 +149,1 @@\n+        threadStarted();\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/untrace\/untrace001\/untrace001a.java","additions":7,"deletions":0,"binary":false,"changes":7,"status":"modified"},{"patch":"@@ -52,1 +52,0 @@\n- *      -jdb.option=-trackallthreads\n@@ -80,0 +79,1 @@\n+    static final String THREAD_STARTED_BREAK = PACKAGE_NAME + \".MyThread.threadStarted\";\n@@ -97,1 +97,33 @@\n-        jdb.receiveReplyFor(JdbCommand.cont);\n+        jdb.setBreakpointInMethod(THREAD_STARTED_BREAK);\n+\n+        \/\/ Continue until the debuggee reaches the LAST_BREAK breakpoint. Each\n+        \/\/ tested thread first hits threadStarted(); receiving that event is\n+        \/\/ what makes a virtual thread visible to jdb with the default debug\n+        \/\/ agent behavior, so count the hits to be sure every tested thread\n+        \/\/ became visible before the lookup below.\n+        int started = 0;\n+        while (true) {\n+            String[] contReply = jdb.receiveReplyFor(JdbCommand.cont);\n+            \/\/ Match the fully qualified method of the stop location as a single\n+            \/\/ token: debuggee output can interleave with jdb's own output and\n+            \/\/ split the \"Breakpoint hit:\" prefix away from the location, so do\n+            \/\/ not require both in the same paragraph. The \"(\" suffix avoids\n+            \/\/ matching jdb's \"Set deferred breakpoint\" messages.\n+            Paragrep contGrep = new Paragrep(contReply);\n+            if (contGrep.find(LAST_BREAK + \"(\") > 0) {\n+                break;\n+            }\n+            if (contGrep.find(THREAD_STARTED_BREAK + \"(\") > 0) {\n+                started++;\n+                continue;\n+            }\n+            failure(\"Stopped at unexpected location, expected \" + LAST_BREAK\n+                    + \" or \" + THREAD_STARTED_BREAK);\n+            for (String line : contReply) {\n+                log.complain(\"reply: \" + line);\n+            }\n+            break;\n+        }\n+        if (started != where006a.numThreads) {\n+            failure(\"Expected \" + where006a.numThreads + \" threadStarted() hits, got: \" + started);\n+        }\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/where\/where006\/where006.java","additions":34,"deletions":2,"binary":false,"changes":36,"status":"modified"},{"patch":"@@ -113,0 +113,6 @@\n+   \/\/ Each tested thread calls this once at startup. The debugger sets a\n+   \/\/ breakpoint here: receiving the event is what makes a virtual thread\n+   \/\/ visible to jdb with the default debug agent behavior, so the test\n+   \/\/ does not need the -trackallthreads option.\n+   static void threadStarted() {}\n+\n@@ -114,0 +120,1 @@\n+       threadStarted();\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/where\/where006\/where006a.java","additions":7,"deletions":0,"binary":false,"changes":7,"status":"modified"},{"patch":"@@ -47,1 +47,0 @@\n- *      -jdb.option=-trackallthreads\n@@ -75,0 +74,1 @@\n+    static final String THREAD_STARTED_BREAK = PACKAGE_NAME + \".MyThread.threadStarted\";\n@@ -86,1 +86,33 @@\n-        reply = jdb.receiveReplyFor(JdbCommand.cont);\n+        jdb.setBreakpointInMethod(THREAD_STARTED_BREAK);\n+\n+        \/\/ Continue until the debuggee reaches the LAST_BREAK breakpoint. Each\n+        \/\/ tested thread first hits threadStarted(); receiving that event is\n+        \/\/ what makes a virtual thread visible to jdb with the default debug\n+        \/\/ agent behavior, so count the hits to be sure every tested thread\n+        \/\/ became visible before the lookup below.\n+        int started = 0;\n+        while (true) {\n+            String[] contReply = jdb.receiveReplyFor(JdbCommand.cont);\n+            \/\/ Match the fully qualified method of the stop location as a single\n+            \/\/ token: debuggee output can interleave with jdb's own output and\n+            \/\/ split the \"Breakpoint hit:\" prefix away from the location, so do\n+            \/\/ not require both in the same paragraph. The \"(\" suffix avoids\n+            \/\/ matching jdb's \"Set deferred breakpoint\" messages.\n+            Paragrep contGrep = new Paragrep(contReply);\n+            if (contGrep.find(LAST_BREAK + \"(\") > 0) {\n+                break;\n+            }\n+            if (contGrep.find(THREAD_STARTED_BREAK + \"(\") > 0) {\n+                started++;\n+                continue;\n+            }\n+            failure(\"Stopped at unexpected location, expected \" + LAST_BREAK\n+                    + \" or \" + THREAD_STARTED_BREAK);\n+            for (String line : contReply) {\n+                log.complain(\"reply: \" + line);\n+            }\n+            break;\n+        }\n+        if (started != wherei001a.numThreads) {\n+            failure(\"Expected \" + wherei001a.numThreads + \" threadStarted() hits, got: \" + started);\n+        }\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/wherei\/wherei001\/wherei001.java","additions":34,"deletions":2,"binary":false,"changes":36,"status":"modified"},{"patch":"@@ -97,0 +97,6 @@\n+    \/\/ Each tested thread calls this once at startup. The debugger sets a\n+    \/\/ breakpoint here: receiving the event is what makes a virtual thread\n+    \/\/ visible to jdb with the default debug agent behavior, so the test\n+    \/\/ does not need the -trackallthreads option.\n+    static void threadStarted() {}\n+\n@@ -98,0 +104,1 @@\n+        threadStarted();\n","filename":"test\/hotspot\/jtreg\/vmTestbase\/nsk\/jdb\/wherei\/wherei001\/wherei001a.java","additions":7,"deletions":0,"binary":false,"changes":7,"status":"modified"}]}