{"files":[{"patch":"@@ -2630,14 +2630,14 @@\n-  \/*\n-   * Sometimes the debuggee changes the thread name, so we need to fetch\n-   * and set it again.\n-   *\/\n-  jvmtiThreadInfo info;\n-  jvmtiError error;\n-\n-  memset(&info, 0, sizeof(info));\n-  error = JVMTI_FUNC_PTR(gdata->jvmti,GetThreadInfo)\n-    (gdata->jvmti, node->thread, &info);\n-  if (info.name != NULL) {\n-    strncpy(node->name, info.name, sizeof(node->name) - 1);\n-    jvmtiDeallocate(info.name);\n-  }\n+    \/*\n+     * Sometimes the debuggee changes the thread name, so we need to fetch\n+     * and set it again.\n+     *\/\n+    jvmtiThreadInfo info;\n+    jvmtiError error;\n+\n+    memset(&info, 0, sizeof(info));\n+    error = JVMTI_FUNC_PTR(gdata->jvmti,GetThreadInfo)\n+        (gdata->jvmti, node->thread, &info);\n+    if (info.name != NULL) {\n+        strncpy(node->name, info.name, sizeof(node->name) - 1);\n+        jvmtiDeallocate(info.name);\n+    }\n@@ -2651,4 +2651,4 @@\n-  jint state = 0;\n-  jvmtiError error = FUNC_PTR(gdata->jvmti,GetThreadState)\n-    (gdata->jvmti, node->thread, &state);\n-  return state;\n+    jint state = 0;\n+    jvmtiError error = FUNC_PTR(gdata->jvmti,GetThreadState)\n+        (gdata->jvmti, node->thread, &state);\n+    return state;\n","filename":"src\/jdk.jdwp.agent\/share\/native\/libjdwp\/threadControl.c","additions":18,"deletions":18,"binary":false,"changes":36,"status":"modified"}]}