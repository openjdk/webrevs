{"files":[{"patch":"@@ -403,1 +403,1 @@\n-         * as a <em>structure violation<\/em> when the operation completes (normally or ith an\n+         * as a <em>structure violation<\/em> when the operation completes (normally or with an\n@@ -433,1 +433,1 @@\n-         * as a <em>structure violation<\/em> when the operation completes (normally or ith an\n+         * as a <em>structure violation<\/em> when the operation completes (normally or with an\n@@ -497,1 +497,1 @@\n-         * as a <em>structure violation<\/em> when the operation completes (normally or ith an\n+         * as a <em>structure violation<\/em> when the operation completes (normally or with an\n@@ -565,1 +565,1 @@\n-     * as a <em>structure violation<\/em> when the operation completes (normally or ith an\n+     * as a <em>structure violation<\/em> when the operation completes (normally or with an\n@@ -601,1 +601,1 @@\n-     * as a <em>structure violation<\/em> when the operation completes (normally or ith an\n+     * as a <em>structure violation<\/em> when the operation completes (normally or with an\n@@ -636,1 +636,1 @@\n-     * as a <em>structure violation<\/em> when the operation completes (normally or ith an\n+     * as a <em>structure violation<\/em> when the operation completes (normally or with an\n","filename":"src\/java.base\/share\/classes\/java\/lang\/ScopedValue.java","additions":6,"deletions":6,"binary":false,"changes":12,"status":"modified"}]}