{"files":[{"patch":"@@ -849,0 +849,2 @@\n+      assert(src.getMode() == Address::base_plus_offset && src.offset() == 0,\n+          \"is_acquire path requires address to be base-only\");\n@@ -855,0 +857,2 @@\n+      assert(src.getMode() == Address::base_plus_offset && src.offset() == 0,\n+          \"is_acquire path requires address to be base-only\");\n@@ -882,0 +886,2 @@\n+      assert(dst.getMode() == Address::base_plus_offset && dst.offset() == 0,\n+          \"is_acquire path requires address to be base-only\");\n@@ -888,0 +894,2 @@\n+      assert(dst.getMode() == Address::base_plus_offset && dst.offset() == 0,\n+          \"is_acquire path requires address to be base-only\");\n","filename":"src\/hotspot\/cpu\/aarch64\/gc\/shenandoah\/shenandoahBarrierSetAssembler_aarch64.cpp","additions":8,"deletions":0,"binary":false,"changes":8,"status":"modified"}]}