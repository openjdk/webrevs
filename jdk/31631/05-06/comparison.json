{"files":[{"patch":"@@ -108,0 +108,1 @@\n+        assert timeout >= -1L;\n@@ -109,4 +110,3 @@\n-        \/\/ pollset_poll timeout is int\n-        int to = (int) Math.min(timeout, Integer.MAX_VALUE);\n-        boolean blocking = (to != 0);\n-        boolean timedPoll = (to > 0);\n+        \/\/ poll timeout is int\n+        int pollTimeout = Math.clamp(timeout, -1, Integer.MAX_VALUE);\n+        boolean blocking = (pollTimeout != 0);\n@@ -121,18 +121,5 @@\n-            do {\n-                long startTime = timedPoll ? System.nanoTime() : 0;\n-                boolean attempted = Blocker.begin(blocking);\n-                try {\n-                    numEntries = Pollset.pollsetPoll(pfd, pollArrayAddress, NUM_POLLCTLEVENTS, to);\n-                } finally {\n-                    Blocker.end(attempted);\n-                }\n-                if (numEntries == IOStatus.INTERRUPTED && timedPoll) {\n-                    \/\/ timed poll interrupted so need to adjust timeout\n-                    long adjust = System.nanoTime() - startTime;\n-                    to -= (int) TimeUnit.NANOSECONDS.toMillis(adjust);\n-                    if (to <= 0) {\n-                        \/\/ timeout expired so no retry\n-                        numEntries = 0;\n-                    }\n-                }\n-            } while (numEntries == IOStatus.INTERRUPTED);\n+            if (blocking && Thread.currentThread().isVirtual()) {\n+                numEntries = compensatingPoll(pollTimeout);\n+            } else {\n+                numEntries = poll(pollTimeout);\n+            }\n@@ -148,0 +135,47 @@\n+    \/**\n+     * Poll for I\/O events.\n+     *\/\n+    private int poll(int pollTimeout) throws IOException {\n+        return Pollset.pollsetPoll(pfd, pollArrayAddress, NUM_POLLCTLEVENTS, pollTimeout);\n+    }\n+\n+    \/**\n+     * Invoked by a virtual thread to poll for I\/O events without releasing its carrier\n+     * thread. The Blocker mechanism is used to arrange for a spare carrier thread to\n+     * be activated if necessary while the blocking operation is in progress.\n+     * The maximum underlying poll is 10s to ensure that the Blocker mechanism keeps\n+     * a spare thread available for the entire duration of the poll. This avoids\n+     * cases where the keep alive timeout kicks in and the spare terminates while\n+     * the poll is in progress.\n+     * @param pollTimeout the poll time or -1 to block indefinitely\n+     *\/\n+    private int compensatingPoll(int pollTimeout) throws IOException {\n+        assert Thread.currentThread().isVirtual() && pollTimeout != 0;\n+        long startTime = System.nanoTime();\n+\n+        \/\/ non-blocking\n+        int numEntries = poll(0);\n+        if (numEntries > 0) {\n+            return numEntries;\n+        }\n+\n+        \/\/ blocking loop\n+        for (;;) {\n+            boolean attempted = Blocker.begin();\n+            try {\n+                long rem = (pollTimeout > 0)\n+                    ? pollTimeout - TimeUnit.NANOSECONDS.toMillis(System.nanoTime() - startTime)\n+                    : Integer.MAX_VALUE;\n+                if (rem <= 0) {\n+                    return 0;\n+                }\n+                numEntries = poll(Math.clamp(rem, 1, 10_000));  \/\/ max 10s\n+                if (numEntries > 0) {\n+                    return numEntries;\n+                }\n+            } finally {\n+                Blocker.end(attempted);\n+            }\n+        }\n+    }\n+\n","filename":"src\/java.base\/aix\/classes\/sun\/nio\/ch\/PollsetSelectorImpl.java","additions":56,"deletions":22,"binary":false,"changes":78,"status":"modified"},{"patch":"@@ -29,0 +29,6 @@\n+\/* @test\n+ * @requires (os.family == \"aix\")\n+ * @library ..\n+ * @run junit\/othervm -Djava.nio.channels.spi.SelectorProvider=sun.nio.ch.PollSelectorProvider BasicAccept\n+ *\/\n+\n","filename":"test\/jdk\/java\/nio\/channels\/Selector\/BasicAccept.java","additions":6,"deletions":0,"binary":false,"changes":6,"status":"modified"},{"patch":"@@ -32,0 +32,6 @@\n+\/* @test\n+ * @requires (os.family == \"aix\")\n+ * @build SelectorUtils\n+ * @run main\/othervm -Djava.nio.channels.spi.SelectorProvider=sun.nio.ch.PollSelectorProvider RegisterDuringSelect\n+ *\/\n+\n","filename":"test\/jdk\/java\/nio\/channels\/Selector\/RegisterDuringSelect.java","additions":6,"deletions":0,"binary":false,"changes":6,"status":"modified"},{"patch":"@@ -35,0 +35,5 @@\n+\/* @test\n+ * @requires (os.family == \"aix\")\n+ * @run junit\/othervm -Djava.nio.channels.spi.SelectorProvider=sun.nio.ch.PollSelectorProvider SelectWithConsumer\n+ *\/\n+\n","filename":"test\/jdk\/java\/nio\/channels\/Selector\/SelectWithConsumer.java","additions":5,"deletions":0,"binary":false,"changes":5,"status":"modified"},{"patch":"@@ -30,0 +30,5 @@\n+\/* @test\n+ * @requires (os.family == \"aix\")\n+ * @run junit\/othervm -Djava.nio.channels.spi.SelectorProvider=sun.nio.ch.PollSelectorProvider UpdateReadyOps\n+ *\/\n+\n","filename":"test\/jdk\/java\/nio\/channels\/Selector\/UpdateReadyOps.java","additions":5,"deletions":0,"binary":false,"changes":5,"status":"modified"},{"patch":"@@ -30,0 +30,6 @@\n+\/* @test\n+ * @requires (os.family == \"aix\")\n+ * @library .. \/test\/lib\n+ * @run main\/othervm -Djava.nio.channels.spi.SelectorProvider=sun.nio.ch.PollSelectorProvider Wakeup\n+ *\/\n+\n","filename":"test\/jdk\/java\/nio\/channels\/Selector\/Wakeup.java","additions":6,"deletions":0,"binary":false,"changes":6,"status":"modified"}]}