{"files":[{"patch":"@@ -695,1 +695,0 @@\n-        var thresholds = IntVector.broadcast(SPECIES_I, threshold);\n@@ -700,1 +699,1 @@\n-            var mask = v.compare(VectorOperators.GE, thresholds);\n+            var mask = v.compare(VectorOperators.GE, threshold);\n@@ -723,1 +722,0 @@\n-        var thresholds = IntVector.broadcast(SPECIES_I64, threshold);\n@@ -728,1 +726,1 @@\n-            var mask = v.compare(VectorOperators.GE, thresholds);\n+            var mask = v.compare(VectorOperators.GE, threshold);\n@@ -733,4 +731,2 @@\n-                int v0 = v.lane(0);\n-                int v1 = v.lane(1);\n-                if (v0 >= threshold) { r[j++] = v0; }\n-                if (v1 >= threshold) { r[j++] = v1; }\n+                if (mask.laneIsSet(0)) { r[j++] = v.lane(0); }\n+                if (mask.laneIsSet(1)) { r[j++] = v.lane(1); }\n@@ -753,1 +749,0 @@\n-        var thresholds = IntVector.broadcast(SPECIES_I128, threshold);\n@@ -758,1 +753,1 @@\n-            var mask = v.compare(VectorOperators.GE, thresholds);\n+            var mask = v.compare(VectorOperators.GE, threshold);\n@@ -763,8 +758,4 @@\n-                int v0 = v.lane(0);\n-                int v1 = v.lane(1);\n-                int v2 = v.lane(2);\n-                int v3 = v.lane(3);\n-                if (v0 >= threshold) { r[j++] = v0; }\n-                if (v1 >= threshold) { r[j++] = v1; }\n-                if (v2 >= threshold) { r[j++] = v2; }\n-                if (v3 >= threshold) { r[j++] = v3; }\n+                if (mask.laneIsSet(0)) { r[j++] = v.lane(0); }\n+                if (mask.laneIsSet(1)) { r[j++] = v.lane(1); }\n+                if (mask.laneIsSet(2)) { r[j++] = v.lane(2); }\n+                if (mask.laneIsSet(3)) { r[j++] = v.lane(3); }\n@@ -787,1 +778,0 @@\n-        var thresholds = IntVector.broadcast(SPECIES_I256, threshold);\n@@ -792,1 +782,1 @@\n-            var mask = v.compare(VectorOperators.GE, thresholds);\n+            var mask = v.compare(VectorOperators.GE, threshold);\n@@ -797,16 +787,8 @@\n-                int v0 = v.lane(0);\n-                int v1 = v.lane(1);\n-                int v2 = v.lane(2);\n-                int v3 = v.lane(3);\n-                int v4 = v.lane(4);\n-                int v5 = v.lane(5);\n-                int v6 = v.lane(6);\n-                int v7 = v.lane(7);\n-                if (v0 >= threshold) { r[j++] = v0; }\n-                if (v1 >= threshold) { r[j++] = v1; }\n-                if (v2 >= threshold) { r[j++] = v2; }\n-                if (v3 >= threshold) { r[j++] = v3; }\n-                if (v4 >= threshold) { r[j++] = v4; }\n-                if (v5 >= threshold) { r[j++] = v5; }\n-                if (v6 >= threshold) { r[j++] = v6; }\n-                if (v7 >= threshold) { r[j++] = v7; }\n+                if (mask.laneIsSet(0)) { r[j++] = v.lane(0); }\n+                if (mask.laneIsSet(1)) { r[j++] = v.lane(1); }\n+                if (mask.laneIsSet(2)) { r[j++] = v.lane(2); }\n+                if (mask.laneIsSet(3)) { r[j++] = v.lane(3); }\n+                if (mask.laneIsSet(4)) { r[j++] = v.lane(4); }\n+                if (mask.laneIsSet(5)) { r[j++] = v.lane(5); }\n+                if (mask.laneIsSet(6)) { r[j++] = v.lane(6); }\n+                if (mask.laneIsSet(7)) { r[j++] = v.lane(7); }\n","filename":"test\/hotspot\/jtreg\/compiler\/vectorization\/VectorAlgorithmsImpl.java","additions":18,"deletions":36,"binary":false,"changes":54,"status":"modified"},{"patch":"@@ -695,1 +695,0 @@\n-        var thresholds = IntVector.broadcast(SPECIES_I, threshold);\n@@ -700,1 +699,1 @@\n-            var mask = v.compare(VectorOperators.GE, thresholds);\n+            var mask = v.compare(VectorOperators.GE, threshold);\n@@ -723,1 +722,0 @@\n-        var thresholds = IntVector.broadcast(SPECIES_I64, threshold);\n@@ -728,1 +726,1 @@\n-            var mask = v.compare(VectorOperators.GE, thresholds);\n+            var mask = v.compare(VectorOperators.GE, threshold);\n@@ -733,4 +731,2 @@\n-                int v0 = v.lane(0);\n-                int v1 = v.lane(1);\n-                if (v0 >= threshold) { r[j++] = v0; }\n-                if (v1 >= threshold) { r[j++] = v1; }\n+                if (mask.laneIsSet(0)) { r[j++] = v.lane(0); }\n+                if (mask.laneIsSet(1)) { r[j++] = v.lane(1); }\n@@ -753,1 +749,0 @@\n-        var thresholds = IntVector.broadcast(SPECIES_I128, threshold);\n@@ -758,1 +753,1 @@\n-            var mask = v.compare(VectorOperators.GE, thresholds);\n+            var mask = v.compare(VectorOperators.GE, threshold);\n@@ -763,8 +758,4 @@\n-                int v0 = v.lane(0);\n-                int v1 = v.lane(1);\n-                int v2 = v.lane(2);\n-                int v3 = v.lane(3);\n-                if (v0 >= threshold) { r[j++] = v0; }\n-                if (v1 >= threshold) { r[j++] = v1; }\n-                if (v2 >= threshold) { r[j++] = v2; }\n-                if (v3 >= threshold) { r[j++] = v3; }\n+                if (mask.laneIsSet(0)) { r[j++] = v.lane(0); }\n+                if (mask.laneIsSet(1)) { r[j++] = v.lane(1); }\n+                if (mask.laneIsSet(2)) { r[j++] = v.lane(2); }\n+                if (mask.laneIsSet(3)) { r[j++] = v.lane(3); }\n@@ -787,1 +778,0 @@\n-        var thresholds = IntVector.broadcast(SPECIES_I256, threshold);\n@@ -792,1 +782,1 @@\n-            var mask = v.compare(VectorOperators.GE, thresholds);\n+            var mask = v.compare(VectorOperators.GE, threshold);\n@@ -797,16 +787,8 @@\n-                int v0 = v.lane(0);\n-                int v1 = v.lane(1);\n-                int v2 = v.lane(2);\n-                int v3 = v.lane(3);\n-                int v4 = v.lane(4);\n-                int v5 = v.lane(5);\n-                int v6 = v.lane(6);\n-                int v7 = v.lane(7);\n-                if (v0 >= threshold) { r[j++] = v0; }\n-                if (v1 >= threshold) { r[j++] = v1; }\n-                if (v2 >= threshold) { r[j++] = v2; }\n-                if (v3 >= threshold) { r[j++] = v3; }\n-                if (v4 >= threshold) { r[j++] = v4; }\n-                if (v5 >= threshold) { r[j++] = v5; }\n-                if (v6 >= threshold) { r[j++] = v6; }\n-                if (v7 >= threshold) { r[j++] = v7; }\n+                if (mask.laneIsSet(0)) { r[j++] = v.lane(0); }\n+                if (mask.laneIsSet(1)) { r[j++] = v.lane(1); }\n+                if (mask.laneIsSet(2)) { r[j++] = v.lane(2); }\n+                if (mask.laneIsSet(3)) { r[j++] = v.lane(3); }\n+                if (mask.laneIsSet(4)) { r[j++] = v.lane(4); }\n+                if (mask.laneIsSet(5)) { r[j++] = v.lane(5); }\n+                if (mask.laneIsSet(6)) { r[j++] = v.lane(6); }\n+                if (mask.laneIsSet(7)) { r[j++] = v.lane(7); }\n","filename":"test\/micro\/org\/openjdk\/bench\/vm\/compiler\/VectorAlgorithmsImpl.java","additions":18,"deletions":36,"binary":false,"changes":54,"status":"modified"}]}