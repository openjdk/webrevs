{"files":[{"patch":"@@ -698,0 +698,3 @@\n+                    if (caretColor == null) {\n+                        caretColor = g.getColor();\n+                    }\n@@ -699,6 +702,10 @@\n-                    int red = (caretColor.getRed() + bg.getRed()) \/ 2;\n-                    int green = (caretColor.getGreen() + bg.getGreen()) \/ 2;\n-                    int blue = (caretColor.getBlue() + bg.getBlue()) \/ 2;\n-                    int alpha = 127;\n-                    Color newCaretColor = new Color(red, green, blue, alpha);\n-                    g.setColor(newCaretColor);\n+                    if (bg == null) {\n+                        g.setColor(caretColor);\n+                    } else {\n+                        int red = (caretColor.getRed() + bg.getRed()) \/ 2;\n+                        int green = (caretColor.getGreen() + bg.getGreen()) \/ 2;\n+                        int blue = (caretColor.getBlue() + bg.getBlue()) \/ 2;\n+                        int alpha = 127;\n+                        Color newCaretColor = new Color(red, green, blue, alpha);\n+                        g.setColor(newCaretColor);\n+                    }\n","filename":"src\/java.desktop\/share\/classes\/javax\/swing\/text\/DefaultCaret.java","additions":13,"deletions":6,"binary":false,"changes":19,"status":"modified"}]}