[{"commit":{"message":"* condition to not run the VM operation was wrong - we need the VM operations throughout the whole concurrent cycle, not only while SATB queues are active"},"files":[{"filename":"src\/hotspot\/share\/gc\/g1\/g1ConcurrentMark.cpp"},{"filename":"src\/hotspot\/share\/gc\/g1\/g1ConcurrentMark.hpp"},{"filename":"src\/hotspot\/share\/gc\/g1\/g1ConcurrentMarkThread.hpp"},{"filename":"src\/hotspot\/share\/gc\/g1\/g1ConcurrentMarkThread.inline.hpp"},{"filename":"src\/hotspot\/share\/gc\/g1\/g1VMOperations.cpp"}],"sha":"6eb629c42dc831459744db396201da50bea88c2f"},{"commit":{"message":"8387265\n\nHi all,\n\n  please review this change that fixes the inconsistency between SATB active enabled flags in the SATB queues and concurrent marking being uncleanly shut down during VM shutdown.\n\nDuring concurrent mark, SATB queues are marked as \"active\" indicating that they (and the upper layers) can accept new objects to be looked for new references to establish the SATB snapshot.\n\nWhen the VM shuts down, G1 uncleanly stops and aborts the VM thread, which means that new items are added to the SATB buffers; at some point they are full and are passed on to be processed. One step of that is filtering out unnecessarily added SATB buffer entries. In this step there is a race:\n\nThread 1: checks if marking is active (it is)\nThread 2: shutdown proceeds and we abort marking, making the concurrent cycle state Idle\nThread 1: the failing assert re-checks that we are still marking fails\n\nThis is basically a day-one bug, just that JDK-8381128 added the assert where we are failing. The assert is good, verifying that we are not accessing marking data structures outside of marking. The values this assert is guarding would be reasonable too.\n\nHowever the actual problem is that during shutdown, we never clean up the SATB buffers when aborting the concurrent cycle threads during shutdown. They still contain entries, and still get items added to.\n\nSo before shutting down marking (in `G1CollectedHeap::stop()`) we need to do that, and we need a safepoint for that.\n\nThis change guarantees the execution of a cleanup safepoint in two ways:\n* (effectively) a Remark pause during VM shutdown will *not* skip the actual safepoint any more, but enter the safe point, and do the cleanup\n* if, during VM shutdown no Remark pause occurred yet, `G1CollectedHeap::stop()` will trigger a dedicated `G1StopMarking` VM operation that performs the cleanup\n\nonly after either of that (or due to races both) of these VM operations occurred, we can let the concurrent cycle go quiescent\/idle.\n\nWith that, SATB buffers are always inactive outside of actual marking, and no elements will be added to them, and that code path will not be executed.\n\nTesting: gha, running the failing test often\n\nThanks,\n  Thomas\n\nTesting:"},"files":[{"filename":"src\/hotspot\/share\/gc\/g1\/g1CollectedHeap.cpp"},{"filename":"src\/hotspot\/share\/gc\/g1\/g1ConcurrentMark.cpp"},{"filename":"src\/hotspot\/share\/gc\/g1\/g1ConcurrentMark.hpp"},{"filename":"src\/hotspot\/share\/gc\/g1\/g1SATBMarkQueueSet.cpp"},{"filename":"src\/hotspot\/share\/gc\/g1\/g1VMOperations.cpp"},{"filename":"src\/hotspot\/share\/gc\/g1\/g1VMOperations.hpp"},{"filename":"src\/hotspot\/share\/runtime\/vmOperation.hpp"}],"sha":"a2d3c04bbf3bfc98cce5475d26d358a535b7aa8f"}]