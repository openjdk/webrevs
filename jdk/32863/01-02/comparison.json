{"files":[{"patch":"@@ -2200,8 +2200,8 @@\n-    if (ra_->C->stub_function() == nullptr) {\n-      \/\/ Emit the entry barrier in a temporary frame before unpacking because\n-      \/\/ it can deopt, which would require packing the scalarized args again.\n-      __ verified_entry(ra_->C, 0);\n-      __ entry_barrier();\n-      int framesize = ra_->C->output()->frame_slots() << LogBytesPerInt;\n-      __ remove_frame(framesize, false);\n-    }\n+    assert(ra_->C->is_method_compilation(), \"should be\");\n+    \/\/ Emit the entry barrier in a temporary frame before unpacking because\n+    \/\/ it can deopt, which would require packing the scalarized args again.\n+    __ verified_entry(ra_->C, 0);\n+    __ entry_barrier();\n+    int framesize = ra_->C->output()->frame_slots() << LogBytesPerInt;\n+    __ remove_frame(framesize, false);\n+\n","filename":"src\/hotspot\/cpu\/aarch64\/aarch64.ad","additions":8,"deletions":8,"binary":false,"changes":16,"status":"modified"},{"patch":"@@ -2586,8 +2586,8 @@\n-    if (ra_->C->stub_function() == nullptr) {\n-      \/\/ Emit the entry barrier in a temporary frame before unpacking because\n-      \/\/ it can deopt, which would require packing the scalarized args again.\n-      __ verified_entry(ra_->C, 0);\n-      __ entry_barrier();\n-      int initial_framesize = ra_->C->output()->frame_size_in_bytes() - 2*wordSize;\n-      __ remove_frame(initial_framesize, false);\n-    }\n+    assert(ra_->C->is_method_compilation(), \"should be\");\n+    \/\/ Emit the entry barrier in a temporary frame before unpacking because\n+    \/\/ it can deopt, which would require packing the scalarized args again.\n+    __ verified_entry(ra_->C, 0);\n+    __ entry_barrier();\n+    int initial_framesize = ra_->C->output()->frame_size_in_bytes() - 2*wordSize;\n+    __ remove_frame(initial_framesize, false);\n+\n","filename":"src\/hotspot\/cpu\/x86\/x86.ad","additions":8,"deletions":8,"binary":false,"changes":16,"status":"modified"}]}