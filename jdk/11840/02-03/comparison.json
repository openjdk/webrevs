{"files":[{"patch":"@@ -35,0 +35,1 @@\n+import java.util.stream.Stream;\n@@ -129,2 +130,1 @@\n-        doubles().forEach(d -> {\n-            float expected = (float) d;\n+        floats().forEach(expected -> {\n@@ -139,2 +139,1 @@\n-        doubles().forEach(d -> {\n-            float expected = (float) d;\n+        floats().forEach(expected -> {\n@@ -190,1 +189,22 @@\n-        return ThreadLocalRandom.current().doubles(ITERATIONS);\n+        return DoubleStream.concat(\n+                ThreadLocalRandom.current().doubles(ITERATIONS),\n+                DoubleStream.of(Double.NaN,\n+                        Double.NEGATIVE_INFINITY,\n+                        Double.POSITIVE_INFINITY,\n+                        Double.MAX_VALUE,\n+                        Double.MIN_VALUE,\n+                        -0.0d\n+                        +0.0d)\n+        );\n+    }\n+    static Stream<Float> floats() {\n+        return Stream.concat(\n+                ThreadLocalRandom.current().doubles(ITERATIONS).mapToObj(d -> (float)d),\n+                Stream.of(Float.NaN,\n+                        Float.NEGATIVE_INFINITY,\n+                        Float.POSITIVE_INFINITY,\n+                        Float.MAX_VALUE,\n+                        Float.MIN_VALUE,\n+                        -0.0f\n+                        +0.0f)\n+        );\n","filename":"test\/jdk\/java\/io\/Bits\/ReadWriteValues.java","additions":25,"deletions":5,"binary":false,"changes":30,"status":"modified"}]}