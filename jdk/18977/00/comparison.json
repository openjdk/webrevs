{"files":[{"patch":"@@ -3067,6 +3067,4 @@\n-        for (int i = 0; i < vsp.length(); i += lsp.length()) {\n-            IntVector vix = IntVector\n-                .fromArray(lsp, indexMap, mapOffset + i)\n-                .add(offset);\n-            VectorIntrinsics.checkIndex(vix, a.length);\n-        }\n+        IntVector vix = IntVector\n+            .fromArray(lsp, indexMap, mapOffset)\n+            .add(offset);\n+        VectorIntrinsics.checkIndex(vix, a.length);\n@@ -3824,6 +3822,4 @@\n-        for (int i = 0; i < vsp.length(); i += lsp.length()) {\n-            IntVector vix = IntVector\n-                .fromArray(lsp, indexMap, mapOffset + i)\n-                .add(offset);\n-            VectorIntrinsics.checkIndex(vix, a.length);\n-        }\n+        IntVector vix = IntVector\n+            .fromArray(lsp, indexMap, mapOffset)\n+            .add(offset);\n+        VectorIntrinsics.checkIndex(vix, a.length);\n","filename":"src\/jdk.incubator.vector\/share\/classes\/jdk\/incubator\/vector\/ByteVector.java","additions":8,"deletions":12,"binary":false,"changes":20,"status":"modified"},{"patch":"@@ -3068,6 +3068,4 @@\n-        for (int i = 0; i < vsp.length(); i += lsp.length()) {\n-            IntVector vix = IntVector\n-                .fromArray(lsp, indexMap, mapOffset + i)\n-                .add(offset);\n-            VectorIntrinsics.checkIndex(vix, a.length);\n-        }\n+        IntVector vix = IntVector\n+            .fromArray(lsp, indexMap, mapOffset)\n+            .add(offset);\n+        VectorIntrinsics.checkIndex(vix, a.length);\n@@ -3810,6 +3808,4 @@\n-        for (int i = 0; i < vsp.length(); i += lsp.length()) {\n-            IntVector vix = IntVector\n-                .fromArray(lsp, indexMap, mapOffset + i)\n-                .add(offset);\n-            VectorIntrinsics.checkIndex(vix, a.length);\n-        }\n+        IntVector vix = IntVector\n+            .fromArray(lsp, indexMap, mapOffset)\n+            .add(offset);\n+        VectorIntrinsics.checkIndex(vix, a.length);\n","filename":"src\/jdk.incubator.vector\/share\/classes\/jdk\/incubator\/vector\/ShortVector.java","additions":8,"deletions":12,"binary":false,"changes":20,"status":"modified"},{"patch":"@@ -3640,6 +3640,4 @@\n-        for (int i = 0; i < vsp.length(); i += lsp.length()) {\n-            IntVector vix = IntVector\n-                .fromArray(lsp, indexMap, mapOffset + i)\n-                .add(offset);\n-            VectorIntrinsics.checkIndex(vix, a.length);\n-        }\n+        IntVector vix = IntVector\n+            .fromArray(lsp, indexMap, mapOffset)\n+            .add(offset);\n+        VectorIntrinsics.checkIndex(vix, a.length);\n@@ -4841,6 +4839,4 @@\n-        for (int i = 0; i < vsp.length(); i += lsp.length()) {\n-            IntVector vix = IntVector\n-                .fromArray(lsp, indexMap, mapOffset + i)\n-                .add(offset);\n-            VectorIntrinsics.checkIndex(vix, a.length);\n-        }\n+        IntVector vix = IntVector\n+            .fromArray(lsp, indexMap, mapOffset)\n+            .add(offset);\n+        VectorIntrinsics.checkIndex(vix, a.length);\n","filename":"src\/jdk.incubator.vector\/share\/classes\/jdk\/incubator\/vector\/X-Vector.java.template","additions":8,"deletions":12,"binary":false,"changes":20,"status":"modified"}]}