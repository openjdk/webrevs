{"files":[{"patch":"@@ -633,0 +633,3 @@\n+     * <p>\n+     * If this segment is {@linkplain MemorySegment#isReadOnly() read-only},\n+     * the returned segment is also {@linkplain MemorySegment#isReadOnly() read-only}.\n@@ -649,0 +652,3 @@\n+     * <p>\n+     * If this segment is {@linkplain MemorySegment#isReadOnly() read-only},\n+     * the returned segment is also {@linkplain MemorySegment#isReadOnly() read-only}.\n@@ -673,0 +679,3 @@\n+     * <p>\n+     * If this segment is {@linkplain MemorySegment#isReadOnly() read-only},\n+     * the returned segment is also {@linkplain MemorySegment#isReadOnly() read-only}.\n@@ -696,0 +705,3 @@\n+     * <p>\n+     * If this segment is {@linkplain MemorySegment#isReadOnly() read-only},\n+     * the returned segment is also {@linkplain MemorySegment#isReadOnly() read-only}.\n@@ -710,2 +722,3 @@\n-     * If, and only if, this segment is {@linkplain MemorySegment#isReadOnly() read-only},\n-     * the returned segment is also {@linkplain MemorySegment#isReadOnly() read-only}\n+     * If this segment is {@linkplain MemorySegment#isReadOnly() read-only},\n+     * the returned segment is also {@linkplain MemorySegment#isReadOnly() read-only}.\n+     *\n@@ -748,2 +761,2 @@\n-     * If, and only if, this segment is {@linkplain MemorySegment#isReadOnly() read-only},\n-     * the returned segment is also {@linkplain MemorySegment#isReadOnly() read-only}\n+     * If this segment is {@linkplain MemorySegment#isReadOnly() read-only},\n+     * the returned segment is also {@linkplain MemorySegment#isReadOnly() read-only}.\n@@ -796,2 +809,2 @@\n-     * If, and only if, this segment is {@linkplain MemorySegment#isReadOnly() read-only},\n-     * the returned segment is also {@linkplain MemorySegment#isReadOnly() read-only}\n+     * If this segment is {@linkplain MemorySegment#isReadOnly() read-only},\n+     * the returned segment is also {@linkplain MemorySegment#isReadOnly() read-only}.\n","filename":"src\/java.base\/share\/classes\/java\/lang\/foreign\/MemorySegment.java","additions":19,"deletions":6,"binary":false,"changes":25,"status":"modified"}]}