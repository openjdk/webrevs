{"files":[{"patch":"@@ -64,1 +64,1 @@\n-\/\/            \"1000000\",\n+            \"1000000\",\n@@ -175,1 +175,1 @@\n-        final ArrayListInt list = new ArrayListInt(size);\n+        final ArrayListInt list = arrayListInt;\n@@ -178,4 +178,4 @@\n-        for (int i = 0; i < size * 10; i++) {\n-            int r = (random.nextInt() & 0x7fffffff);    \/\/ positive\n-            if ((list.size() > 0 && random.nextBoolean()) || list.size() == size) {\n-                list.remove(r % list.size());\n+        for (int i = 0; i < 1000; i++) {\n+            int ndx = (random.nextInt() & 0x7fffffff) % list.size();    \/\/ positive\n+            if (list.size() == size) {\n+                list.remove(ndx);\n@@ -183,4 +183,1 @@\n-                if (list.size() == 0)\n-                    list.add(r);\n-                else\n-                    list.add(r % list.size(), r);\n+                list.add(ndx);\n@@ -188,1 +185,1 @@\n-            sum += r;\n+            sum += ndx;\n@@ -197,4 +194,4 @@\n-        for (int i = 0; i < size * 10; i++) {\n-            int r = (random.nextInt() & 0x7fffffff);    \/\/ positive\n-            if ((list.size() > 0 && random.nextBoolean()) || list.size() == size) {\n-                list.remove(r % list.size());\n+        for (int i = 0; i < 1000; i++) {\n+            int ndx = (random.nextInt() & 0x7fffffff) % list.size();    \/\/ positive\n+            if (list.size() == size) {\n+                list.remove(ndx);\n@@ -202,5 +199,1 @@\n-                PrimitiveInt v = new PrimitiveInt(r);\n-                if (list.size() == 0)\n-                    list.add(v);\n-                else\n-                    list.add(r % list.size(), v);\n+                list.add(ndx, new PrimitiveInt(ndx));\n@@ -208,1 +201,1 @@\n-            sum += r;\n+            sum += ndx;\n@@ -217,4 +210,4 @@\n-        for (int i = 0; i < size * 10; i++) {\n-            int r = (random.nextInt() & 0x7fffffff);    \/\/ positive\n-            if ((list.size() > 0 && random.nextBoolean()) || list.size() == size) {\n-                list.remove(r % list.size());\n+        for (int i = 0; i < 1000; i++) {\n+            int ndx = (random.nextInt() & 0x7fffffff) % list.size();    \/\/ positive\n+            if (list.size() == size) {\n+                list.remove(ndx);\n@@ -222,4 +215,1 @@\n-                if (list.size() == 0)\n-                    list.add(r);\n-                else\n-                    list.add(r % list.size(), r);\n+                list.add(ndx);\n@@ -227,1 +217,1 @@\n-            sum += r;\n+            sum += ndx;\n@@ -237,4 +227,5 @@\n-        for (int i = 0; i < size * 10; i++) {\n-            int r = (random.nextInt() & 0x7fffffff);    \/\/ positive\n-            if ((list.size() > 0 && random.nextBoolean()) || list.size() == size) {\n-                list.remove(r % list.size());\n+\n+        for (int i = 0; i < 1000; i++) {\n+            int ndx = (random.nextInt() & 0x7fffffff) % list.size();    \/\/ positive\n+            if (list.size() == size) {\n+                list.remove(ndx);\n@@ -242,5 +233,1 @@\n-                PrimitiveInt v = new PrimitiveInt(r);\n-                if (list.size() == 0)\n-                    list.add(v);\n-                else\n-                    list.add(r % list.size(), v);\n+                list.add(ndx, new PrimitiveInt(ndx));\n@@ -248,1 +235,1 @@\n-            sum += r;\n+            sum += ndx;\n","filename":"test\/micro\/org\/openjdk\/bench\/valhalla\/sandbox\/corelibs\/ArrayListOfIntBench.java","additions":27,"deletions":40,"binary":false,"changes":67,"status":"modified"}]}