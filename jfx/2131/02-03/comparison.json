{"files":[{"patch":"@@ -36,0 +36,1 @@\n+import java.util.Objects;\n@@ -1707,1 +1708,1 @@\n-        final List<TablePosition> prevState = selectionModel == null ?\n+        final List<TablePosition<S,?>> prevState = selectionModel == null ?\n@@ -1709,1 +1710,1 @@\n-                new ArrayList<>(selectionModel.getSelectedCells());\n+                new ArrayList<>((ObservableList<TablePosition<S,?>>)(Object)selectionModel.getSelectedCells());\n@@ -1733,1 +1734,1 @@\n-        } else {\n+        } else if (prevState != null) {\n@@ -1742,31 +1743,14 @@\n-                if (prevState != null) {\n-                    \/\/ the sort operation effectively permutates the selectedCells list,\n-                    \/\/ but we cannot fire a permutation event as we are talking about\n-                    \/\/ TablePosition's changing (which may reside in the same list\n-                    \/\/ position before and after the sort). Therefore, we fire\n-                    \/\/ add\/remove events for each contiguous range of positions that changed.\n-                    int prevSize = prevState.size();\n-                    int newSize = newState.size();\n-                    int minSize = Math.min(prevSize, newSize);\n-                    for (int index = 0; index < minSize; index++) {\n-                        \/\/ Advance to the next position where prevState and newState are not equal.\n-                        for (; index < minSize && prevState.get(index).equals(newState.get(index)); index++);\n-                        int from = index;\n-                        if (from >= minSize) {\n-                            break;\n-                        }\n-                        \/\/ Advance to the next position where prevState and newState are equal.\n-                        for (index++; index < minSize && !prevState.get(index).equals(newState.get(index)); index++);\n-                        \/\/ The positions at [from, index) in prevState were replaced by the corresponding positions in newState.\n-                        List<TablePosition<S, ?>> removed = (List<TablePosition<S, ?>>) (List<?>) prevState.subList(from, index);\n-                        ListChangeListener.Change<TablePosition<S, ?>> c =\n-                                new NonIterableChange.GenericAddRemoveChange<>(from, index, removed, newState);\n-                        sm.fireCustomSelectedCellsListChangeEvent(c);\n-                    }\n-                    if (prevSize != newSize) {\n-                        \/\/ The positions at [minSize, prevSize) in prevState were removed. If prevSize == minSize, the range is empty.\n-                        \/\/ The positions at [minSize, newSize) in newState were added. If newSize == minSize, the range is empty.\n-                        List<TablePosition<S, ?>> removed = (List<TablePosition<S, ?>>) (List<?>) prevState.subList(minSize, prevSize);\n-                        ListChangeListener.Change<TablePosition<S, ?>> c =\n-                                new NonIterableChange.GenericAddRemoveChange<>(minSize, newSize, removed, newState);\n-                        sm.fireCustomSelectedCellsListChangeEvent(c);\n+                \/\/ the sort operation effectively permutates the selectedCells list,\n+                \/\/ but we cannot fire a permutation event as we are talking about\n+                \/\/ TablePosition's changing (which may reside in the same list\n+                \/\/ position before and after the sort). Therefore, we fire\n+                \/\/ add\/remove events for each contiguous range of positions that changed.\n+                int prevSize = prevState.size();\n+                int newSize = newState.size();\n+                int minSize = Math.min(prevSize, newSize);\n+                for (int index = 0; index < minSize; index++) {\n+                    \/\/ Advance to the next position where prevState and newState are not equal.\n+                    for (; index < minSize && Objects.equals(prevState.get(index), newState.get(index)); index++);\n+                    int from = index;\n+                    if (from >= minSize) {\n+                        break;\n@@ -1774,0 +1758,15 @@\n+                    \/\/ Advance to the next position where prevState and newState are equal.\n+                    for (index++; index < minSize && !Objects.equals(prevState.get(index), newState.get(index)); index++);\n+                    \/\/ The positions at [from, index) in prevState were replaced by the corresponding positions in newState.\n+                    List<TablePosition<S, ?>> removed = prevState.subList(from, index);\n+                    ListChangeListener.Change<TablePosition<S, ?>> c =\n+                            new NonIterableChange.GenericAddRemoveChange<>(from, index, removed, newState);\n+                    sm.fireCustomSelectedCellsListChangeEvent(c);\n+                }\n+                if (prevSize != newSize) {\n+                    \/\/ The positions at [minSize, prevSize) in prevState were removed. If prevSize == minSize, the range is empty.\n+                    \/\/ The positions at [minSize, newSize) in newState were added. If newSize == minSize, the range is empty.\n+                    List<TablePosition<S, ?>> removed = prevSize == minSize ? List.of() : prevState.subList(minSize, prevSize);\n+                    ListChangeListener.Change<TablePosition<S, ?>> c =\n+                            new NonIterableChange.GenericAddRemoveChange<>(minSize, newSize, removed, newState);\n+                    sm.fireCustomSelectedCellsListChangeEvent(c);\n","filename":"modules\/javafx.controls\/src\/main\/java\/javafx\/scene\/control\/TableView.java","additions":33,"deletions":34,"binary":false,"changes":67,"status":"modified"},{"patch":"@@ -39,0 +39,1 @@\n+import java.util.Objects;\n@@ -2042,1 +2043,1 @@\n-        } else {\n+        } else if (prevState != null) {\n@@ -2051,31 +2052,14 @@\n-                if (prevState != null) {\n-                    \/\/ the sort operation effectively permutates the selectedCells list,\n-                    \/\/ but we cannot fire a permutation event as we are talking about\n-                    \/\/ TreeTablePosition's changing (which may reside in the same list\n-                    \/\/ position before and after the sort). Therefore, we fire\n-                    \/\/ add\/remove events for each contiguous range of positions that changed.\n-                    int prevSize = prevState.size();\n-                    int newSize = newState.size();\n-                    int minSize = Math.min(prevSize, newSize);\n-                    for (int index = 0; index < minSize; index++) {\n-                        \/\/ Advance to the next position where prevState and newState are not equal.\n-                        for (; index < minSize && prevState.get(index).equals(newState.get(index)); index++);\n-                        int from = index;\n-                        if (from >= minSize) {\n-                            break;\n-                        }\n-                        \/\/ Advance to the next position where prevState and newState are equal.\n-                        for (index++; index < minSize && !prevState.get(index).equals(newState.get(index)); index++);\n-                        \/\/ The positions at [from, index) in prevState were replaced by the corresponding positions in newState.\n-                        List<TreeTablePosition<S, ?>> removed = (List<TreeTablePosition<S, ?>>) (List<?>) prevState.subList(from, index);\n-                        ListChangeListener.Change<TreeTablePosition<S, ?>> c =\n-                                new NonIterableChange.GenericAddRemoveChange<>(from, index, removed, newState);\n-                        sm.fireCustomSelectedCellsListChangeEvent(c);\n-                    }\n-                    if (prevSize != newSize) {\n-                        \/\/ The positions at [minSize, prevSize) in prevState were removed. If prevSize == minSize, the range is empty.\n-                        \/\/ The positions at [minSize, newSize) in newState were added. If newSize == minSize, the range is empty.\n-                        List<TreeTablePosition<S, ?>> removed = (List<TreeTablePosition<S, ?>>) (List<?>) prevState.subList(minSize, prevSize);\n-                        ListChangeListener.Change<TreeTablePosition<S, ?>> c =\n-                                new NonIterableChange.GenericAddRemoveChange<>(minSize, newSize, removed, newState);\n-                        sm.fireCustomSelectedCellsListChangeEvent(c);\n+                \/\/ the sort operation effectively permutates the selectedCells list,\n+                \/\/ but we cannot fire a permutation event as we are talking about\n+                \/\/ TreeTablePosition's changing (which may reside in the same list\n+                \/\/ position before and after the sort). Therefore, we fire\n+                \/\/ add\/remove events for each contiguous range of positions that changed.\n+                int prevSize = prevState.size();\n+                int newSize = newState.size();\n+                int minSize = Math.min(prevSize, newSize);\n+                for (int index = 0; index < minSize; index++) {\n+                    \/\/ Advance to the next position where prevState and newState are not equal.\n+                    for (; index < minSize && Objects.equals(prevState.get(index), newState.get(index)); index++);\n+                    int from = index;\n+                    if (from >= minSize) {\n+                        break;\n@@ -2083,0 +2067,15 @@\n+                    \/\/ Advance to the next position where prevState and newState are equal.\n+                    for (index++; index < minSize && !Objects.equals(prevState.get(index), newState.get(index)); index++);\n+                    \/\/ The positions at [from, index) in prevState were replaced by the corresponding positions in newState.\n+                    List<TreeTablePosition<S, ?>> removed = prevState.subList(from, index);\n+                    ListChangeListener.Change<TreeTablePosition<S, ?>> c =\n+                            new NonIterableChange.GenericAddRemoveChange<>(from, index, removed, newState);\n+                    sm.fireCustomSelectedCellsListChangeEvent(c);\n+                }\n+                if (prevSize != newSize) {\n+                    \/\/ The positions at [minSize, prevSize) in prevState were removed. If prevSize == minSize, the range is empty.\n+                    \/\/ The positions at [minSize, newSize) in newState were added. If newSize == minSize, the range is empty.\n+                    List<TreeTablePosition<S, ?>> removed = prevSize == minSize ? List.of() : prevState.subList(minSize, prevSize);\n+                    ListChangeListener.Change<TreeTablePosition<S, ?>> c =\n+                            new NonIterableChange.GenericAddRemoveChange<>(minSize, newSize, removed, newState);\n+                    sm.fireCustomSelectedCellsListChangeEvent(c);\n","filename":"modules\/javafx.controls\/src\/main\/java\/javafx\/scene\/control\/TreeTableView.java","additions":31,"deletions":32,"binary":false,"changes":63,"status":"modified"},{"patch":"@@ -4051,2 +4051,2 @@\n-        assertEquals(List.of(0, 1),  removedLists.get(0).stream().map(TablePosition::getRow).toList());\n-        assertEquals(List.of(5, 6),  removedLists.get(1).stream().map(TablePosition::getRow).toList());\n+        assertEquals(List.of(0, 1), removedLists.get(0).stream().map(TablePosition::getRow).toList());\n+        assertEquals(List.of(5, 6), removedLists.get(1).stream().map(TablePosition::getRow).toList());\n@@ -4088,1 +4088,1 @@\n-        table.sort();\n+        table.getSortOrder().add(col);\n@@ -4105,0 +4105,1 @@\n+    }\n@@ -4106,3 +4107,9 @@\n-        froms.clear();\n-        tos.clear();\n-        removedLists.clear();\n+    @Test void testSortFiresAddedEventForSelectionSizeMismatch() {\n+        TableColumn<String, String> col = new TableColumn<>(\"col\");\n+        col.setSortType(ASCENDING);\n+        col.setCellValueFactory(param -> new ReadOnlyObjectWrapper<>(param.getValue()));\n+\n+        TableView<String> table = new TableView<>();\n+        table.setItems(FXCollections.observableArrayList(\"a\", \"b\", \"c\"));\n+        table.getColumns().add(col);\n+        table.getSelectionModel().setSelectionMode(SelectionMode.MULTIPLE);\n@@ -4117,1 +4124,15 @@\n-        table.sort();\n+        table.getSelectionModel().clearAndSelect(0);\n+\n+        List<Integer> froms = new ArrayList<>();\n+        List<Integer> tos = new ArrayList<>();\n+        List<List<TablePosition>> removedLists = new ArrayList<>();\n+        table.getSelectionModel().getSelectedCells().addListener((ListChangeListener<TablePosition>) c -> {\n+            while (c.next()) {\n+                froms.add(c.getFrom());\n+                tos.add(c.getTo());\n+                removedLists.add(new ArrayList<>(c.getRemoved()));\n+            }\n+        });\n+\n+        stageLoader = new StageLoader(table);\n+        table.getSortOrder().add(col);\n","filename":"modules\/javafx.controls\/src\/test\/java\/test\/javafx\/scene\/control\/TableViewTest.java","additions":28,"deletions":7,"binary":false,"changes":35,"status":"modified"},{"patch":"@@ -4353,2 +4353,2 @@\n-        assertEquals(List.of(0, 1),  removedLists.get(0).stream().map(TreeTablePosition::getRow).toList());\n-        assertEquals(List.of(5, 6),  removedLists.get(1).stream().map(TreeTablePosition::getRow).toList());\n+        assertEquals(List.of(0, 1), removedLists.get(0).stream().map(TreeTablePosition::getRow).toList());\n+        assertEquals(List.of(5, 6), removedLists.get(1).stream().map(TreeTablePosition::getRow).toList());\n@@ -4396,1 +4396,1 @@\n-        table.sort();\n+        table.getSortOrder().add(col);\n@@ -4413,0 +4413,7 @@\n+    }\n+\n+    @Test void testSortFiresAddedEventForSelectionSizeMismatch() {\n+        TreeItem<String> root = new TreeItem<>();\n+        root.getChildren().add(new TreeItem<>(\"a\"));\n+        root.getChildren().add(new TreeItem<>(\"b\"));\n+        root.getChildren().add(new TreeItem<>(\"c\"));\n@@ -4414,3 +4421,9 @@\n-        froms.clear();\n-        tos.clear();\n-        removedLists.clear();\n+        TreeTableColumn<String, String> col = new TreeTableColumn<>(\"col\");\n+        col.setSortType(ASCENDING);\n+        col.setCellValueFactory(param -> new ReadOnlyObjectWrapper<>(param.getValue().getValue()));\n+\n+        TreeTableView<String> table = new TreeTableView<>();\n+        table.setShowRoot(false);\n+        table.setRoot(root);\n+        table.getColumns().add(col);\n+        table.getSelectionModel().setSelectionMode(SelectionMode.MULTIPLE);\n@@ -4425,1 +4438,15 @@\n-        table.sort();\n+        table.getSelectionModel().clearAndSelect(0);\n+\n+        List<Integer> froms = new ArrayList<>();\n+        List<Integer> tos = new ArrayList<>();\n+        List<List<TreeTablePosition>> removedLists = new ArrayList<>();\n+        table.getSelectionModel().getSelectedCells().addListener((ListChangeListener<TreeTablePosition>) c -> {\n+            while (c.next()) {\n+                froms.add(c.getFrom());\n+                tos.add(c.getTo());\n+                removedLists.add(new ArrayList<>(c.getRemoved()));\n+            }\n+        });\n+\n+        stageLoader = new StageLoader(table);\n+        table.getSortOrder().add(col);\n","filename":"modules\/javafx.controls\/src\/test\/java\/test\/javafx\/scene\/control\/TreeTableViewTest.java","additions":34,"deletions":7,"binary":false,"changes":41,"status":"modified"}]}