{"files":[{"patch":"@@ -50,2 +50,5 @@\n-    ASSERT(ec == ExceptionCode::TypeError);\n-\n+#if ASSERT_ENABLED\n+    if (ec != ExceptionCode::TypeError) {\n+        WTFLogAlways(\"Unexpected ExceptionCode: %d\", static_cast<int>(ec));\n+    }\n+#endif\n","filename":"modules\/javafx.web\/src\/main\/native\/Source\/WebCore\/bindings\/java\/JavaDOMUtils.cpp","additions":5,"deletions":2,"binary":false,"changes":7,"status":"modified"},{"patch":"@@ -1512,2 +1512,4 @@\n-    ASSERT(frame->document()); \/\/ Client calls shouldn't be made when the frame is in inconsistent state.\n-    if (auto* localFrame = dynamicDowncast<LocalFrame>(frame.get()))\n+\n+    if (auto* localFrame = dynamicDowncast<LocalFrame>(frame.get())) {\n+        \/\/ check LocalFrame first, then access document-related state\n+        ASSERT(localFrame->document()); \/\/ Client calls shouldn't be made when the frame is in inconsistent state.\n@@ -1515,0 +1517,1 @@\n+    }\n@@ -1530,2 +1533,3 @@\n-    ASSERT(frame->document()); \/\/ Client calls shouldn't be made when the frame is in inconsistent state.\n-        if (auto* localFrame = dynamicDowncast<LocalFrame>(frame.get()))\n+    if (auto* localFrame = dynamicDowncast<LocalFrame>(frame.get())) {\n+        \/\/ check LocalFrame first, then access document-related state\n+        ASSERT(localFrame->document()); \/\/ Client calls shouldn't be made when the frame is in inconsistent state.\n@@ -1533,0 +1537,1 @@\n+    }\n","filename":"modules\/javafx.web\/src\/main\/native\/Source\/WebCore\/page\/LocalDOMWindow.cpp","additions":9,"deletions":4,"binary":false,"changes":13,"status":"modified"}]}