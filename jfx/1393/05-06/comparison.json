{"files":[{"patch":"@@ -62,1 +62,1 @@\n-     * Verifies that alt-shortcut-RIGHT\/LEFT scrolls horizontally in LTR orientation.\n+     * Verifies that alt-shortcut-RIGHT\/LEFT keys scroll horizontally in LTR orientation.\n@@ -91,1 +91,1 @@\n-     * Verifies that alt-shortcut-RIGHT\/LEFT scrolls horizontally in RTL orientation.\n+     * Verifies that alt-shortcut-RIGHT\/LEFT keys scroll horizontally in RTL orientation.\n@@ -119,0 +119,28 @@\n+    \/**\n+     * Verifies that alt-shortcut-UP\/DOWN keys scroll vertically.\n+     *\/\n+    @Test\n+    public void testVerticalScrollKeyboard() {\n+        AtomicReference<Double> pos = new AtomicReference<>();\n+        execute(\n+            exe(() -> {\n+                control.setMaxHeight(50);\n+                double w = vsb().getValue();\n+                pos.set(w);\n+            }),\n+            key(KeyCode.DOWN, KeyModifier.ALT, KeyModifier.getShortcutKey()),\n+            exe(() -> {\n+                double w = vsb().getValue();\n+                \/\/ should have scrolled\n+                Assertions.assertTrue(pos.get() < w);\n+                pos.set(w);\n+            }),\n+            key(KeyCode.UP, KeyModifier.ALT, KeyModifier.getShortcutKey()),\n+            exe(() -> {\n+                double w = vsb().getValue();\n+                \/\/ should have scrolled\n+                Assertions.assertTrue(pos.get() > w);\n+            })\n+        );\n+    }\n+\n@@ -123,0 +151,5 @@\n+\n+    private ScrollBar vsb() {\n+        var f = VirtualFlowShim.getVirtualFlow(control.getSkin());\n+        return VirtualFlowShim.getVBar(f);\n+    }\n","filename":"modules\/javafx.controls\/src\/test\/java\/test\/javafx\/scene\/control\/behavior\/ListViewBehaviorTest.java","additions":35,"deletions":2,"binary":false,"changes":37,"status":"modified"},{"patch":"@@ -73,1 +73,1 @@\n-     * Verifies that alt-shortcut-RIGHT\/LEFT scrolls horizontally in LTR orientation.\n+     * Verifies that alt-shortcut-RIGHT\/LEFT keys scroll horizontally in LTR orientation.\n@@ -102,1 +102,1 @@\n-     * Verifies that alt-shortcut-RIGHT\/LEFT scrolls horizontally in RTL orientation.\n+     * Verifies that alt-shortcut-RIGHT\/LEFT keys scroll horizontally in RTL orientation.\n@@ -130,0 +130,28 @@\n+    \/**\n+     * Verifies that alt-shortcut-UP\/DOWN keys scroll vertically.\n+     *\/\n+    @Test\n+    public void testVerticalScrollKeyboard() {\n+        AtomicReference<Double> pos = new AtomicReference<>();\n+        execute(\n+            exe(() -> {\n+                control.setMaxHeight(50);\n+                double w = vsb().getValue();\n+                pos.set(w);\n+            }),\n+            key(KeyCode.DOWN, KeyModifier.ALT, KeyModifier.getShortcutKey()),\n+            exe(() -> {\n+                double w = vsb().getValue();\n+                \/\/ should have scrolled\n+                Assertions.assertTrue(pos.get() < w);\n+                pos.set(w);\n+            }),\n+            key(KeyCode.UP, KeyModifier.ALT, KeyModifier.getShortcutKey()),\n+            exe(() -> {\n+                double w = vsb().getValue();\n+                \/\/ should have scrolled\n+                Assertions.assertTrue(pos.get() > w);\n+            })\n+        );\n+    }\n+\n@@ -134,0 +162,5 @@\n+\n+    private ScrollBar vsb() {\n+        var f = VirtualFlowShim.getVirtualFlow(control.getSkin());\n+        return VirtualFlowShim.getVBar(f);\n+    }\n","filename":"modules\/javafx.controls\/src\/test\/java\/test\/javafx\/scene\/control\/behavior\/TableViewBehaviorTest.java","additions":35,"deletions":2,"binary":false,"changes":37,"status":"modified"},{"patch":"@@ -48,1 +48,8 @@\n-        TreeItem<Integer> root = new TreeItem(1);\n+        TreeItem<Integer> root = new TreeItem<>(1);\n+        root.setExpanded(true);\n+        root.getChildren().addAll(\n+            new TreeItem<>(2),\n+            new TreeItem<>(3),\n+            new TreeItem<>(4)\n+        );\n+\n@@ -73,1 +80,1 @@\n-     * Verifies that alt-shortcut-RIGHT\/LEFT scrolls horizontally in LTR orientation.\n+     * Verifies that alt-shortcut-RIGHT\/LEFT keys scroll horizontally in LTR orientation.\n@@ -102,1 +109,1 @@\n-     * Verifies that alt-shortcut-RIGHT\/LEFT scrolls horizontally in RTL orientation.\n+     * Verifies that alt-shortcut-RIGHT\/LEFT keys scroll horizontally in RTL orientation.\n@@ -130,0 +137,28 @@\n+    \/**\n+     * Verifies that alt-shortcut-UP\/DOWN keys scroll vertically.\n+     *\/\n+    @Test\n+    public void testVerticalScrollKeyboard() {\n+        AtomicReference<Double> pos = new AtomicReference<>();\n+        execute(\n+            exe(() -> {\n+                control.setMaxHeight(50);\n+                double w = vsb().getValue();\n+                pos.set(w);\n+            }),\n+            key(KeyCode.DOWN, KeyModifier.ALT, KeyModifier.getShortcutKey()),\n+            exe(() -> {\n+                double w = vsb().getValue();\n+                \/\/ should have scrolled\n+                Assertions.assertTrue(pos.get() < w);\n+                pos.set(w);\n+            }),\n+            key(KeyCode.UP, KeyModifier.ALT, KeyModifier.getShortcutKey()),\n+            exe(() -> {\n+                double w = vsb().getValue();\n+                \/\/ should have scrolled\n+                Assertions.assertTrue(pos.get() > w);\n+            })\n+        );\n+    }\n+\n@@ -134,0 +169,5 @@\n+\n+    private ScrollBar vsb() {\n+        var f = VirtualFlowShim.getVirtualFlow(control.getSkin());\n+        return VirtualFlowShim.getVBar(f);\n+    }\n","filename":"modules\/javafx.controls\/src\/test\/java\/test\/javafx\/scene\/control\/behavior\/TreeTableViewBehaviorTest.java","additions":43,"deletions":3,"binary":false,"changes":46,"status":"modified"},{"patch":"@@ -47,1 +47,8 @@\n-        TreeItem<Integer> root = new TreeItem(1);\n+        TreeItem<Integer> root = new TreeItem<>(1);\n+                root.setExpanded(true);\n+        root.getChildren().addAll(\n+            new TreeItem<>(2),\n+            new TreeItem<>(3),\n+            new TreeItem<>(4)\n+        );\n+\n@@ -68,1 +75,1 @@\n-     * Verifies that alt-shortcut-RIGHT\/LEFT scrolls horizontally in LTR orientation.\n+     * Verifies that alt-shortcut-RIGHT\/LEFT keys scroll horizontally in LTR orientation.\n@@ -97,1 +104,1 @@\n-     * Verifies that alt-shortcut-RIGHT\/LEFT scrolls horizontally in RTL orientation.\n+     * Verifies that alt-shortcut-RIGHT\/LEFT keys scroll horizontally in RTL orientation.\n@@ -125,0 +132,28 @@\n+    \/**\n+     * Verifies that alt-shortcut-UP\/DOWN keys scroll vertically.\n+     *\/\n+    @Test\n+    public void testVerticalScrollKeyboard() {\n+        AtomicReference<Double> pos = new AtomicReference<>();\n+        execute(\n+            exe(() -> {\n+                control.setMaxHeight(50);\n+                double w = vsb().getValue();\n+                pos.set(w);\n+            }),\n+            key(KeyCode.DOWN, KeyModifier.ALT, KeyModifier.getShortcutKey()),\n+            exe(() -> {\n+                double w = vsb().getValue();\n+                \/\/ should have scrolled\n+                Assertions.assertTrue(pos.get() < w);\n+                pos.set(w);\n+            }),\n+            key(KeyCode.UP, KeyModifier.ALT, KeyModifier.getShortcutKey()),\n+            exe(() -> {\n+                double w = vsb().getValue();\n+                \/\/ should have scrolled\n+                Assertions.assertTrue(pos.get() > w);\n+            })\n+        );\n+    }\n+\n@@ -129,0 +164,5 @@\n+\n+    private ScrollBar vsb() {\n+        var f = VirtualFlowShim.getVirtualFlow(control.getSkin());\n+        return VirtualFlowShim.getVBar(f);\n+    }\n","filename":"modules\/javafx.controls\/src\/test\/java\/test\/javafx\/scene\/control\/behavior\/TreeViewBehaviorTest.java","additions":43,"deletions":3,"binary":false,"changes":46,"status":"modified"}]}