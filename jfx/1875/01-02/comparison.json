{"files":[{"patch":"@@ -640,1 +640,1 @@\n-     * If loading in the background is requested, then the progress property can\n+     * If loading in the background is requested, then the {@link #progressProperty() progress} property can\n@@ -685,1 +685,1 @@\n-     * If loading in the background is requested, then the progress property can\n+     * If loading in the background is requested, then the {@link #progressProperty() progress} property can\n@@ -734,1 +734,1 @@\n-     * If loading in the background is requested, then the progress property can\n+     * If loading in the background is requested, then the {@link #progressProperty() progress} property can\n@@ -776,1 +776,1 @@\n-     * If loading in the background is requested, then the progress property can\n+     * If loading in the background is requested, then the {@link #progressProperty() progress} property can\n@@ -1136,5 +1136,1 @@\n-                return loadImageAsync(\n-                    this, url,\n-                    requestedWidth, requestedHeight,\n-                    preserveRatio, smooth\n-                );\n+                return loadImageAsync(this, url, requestedWidth, requestedHeight, preserveRatio, smooth);\n@@ -1143,5 +1139,1 @@\n-            return loadImageAsync(\n-                this, inputSource,\n-                requestedWidth, requestedHeight,\n-                preserveRatio, smooth\n-            );\n+            return loadImageAsync(this, inputSource, requestedWidth, requestedHeight, preserveRatio, smooth);\n","filename":"modules\/javafx.graphics\/src\/main\/java\/javafx\/scene\/image\/Image.java","additions":6,"deletions":14,"binary":false,"changes":20,"status":"modified"}]}