{"files":[{"patch":"@@ -78,1 +78,1 @@\n-            return \"mat2\";\n+            return \"mat3\";\n@@ -86,4 +86,0 @@\n-  \/\/  static public String mapVecName(String vname) {\n-    \/\/    return \"vec\" + vname.substring(3);\n-   \/\/ }\n-\n@@ -113,0 +109,3 @@\n+                case \"inversesqrt\" ->\n+                    bldr.id(\"rsqrt\").paren(_ ->\n+                            bldr.commaSpaceSeparated(invoke.operandsAsResults(), operand -> bldr.recurse(operand.op())));\n@@ -159,2 +158,9 @@\n-                    && IfaceValue.mat.class.isAssignableFrom(c)){\n-                        bldr.id(\"vec2_mul_vec2_mat2\").paren(_->bldr.recurse(invoke.opFromFirstOperandOrNull()).csp().recurse(invoke.opFromOperandNOrNull(1)));\n+                    && mat2.class.isAssignableFrom(c)) {\n+                    bldr.id(\"vec2_mul_vec2_mat2\").paren(_ -> bldr.recurse(invoke.opFromFirstOperandOrNull()).csp().recurse(invoke.opFromOperandNOrNull(1)));\n+                }else    if (invoke.named(\"mul\")\n+                            && invoke.operandCount() == 2\n+                            && invoke.resultFromOperandNOrNull(1) instanceof Op.Result r\n+                            && r.type() instanceof ClassType cte\n+                            && OpHelper.classTypeToTypeOrThrow(invoke.lookup(), cte) instanceof Class<?> c\n+                            && mat3.class.isAssignableFrom(c)){\n+                        bldr.id(\"vec3_mul_vec3_mat3\").paren(_->bldr.recurse(invoke.opFromFirstOperandOrNull()).csp().recurse(invoke.opFromOperandNOrNull(1)));\n@@ -244,1 +250,1 @@\n-                                                        l.y*r._10+l.y*r._11\n+                                        l.y*r._10+l.y*r._11\n@@ -247,1 +253,11 @@\n-\n+        builder.func(\n+                _->builder.type(\"vec3\"),\n+                \"vec3_mul_vec3_mat3\",\n+                _->builder.type(\"vec3\").sp().id(\"l\").csp().type(\"mat3\").sp().id(\"r\"),\n+                _->builder.returnKeyword().sp().paren(_->builder.type(\"vec3\")).paren(_->\n+                        builder.preformatted(\"\"\"\n+                                l.x*r._00+l.x*r._01+l.x*r._02,\n+                                l.y*r._10+l.y*r._11+l.y*r._12,\n+                                l.z*r._20+l.z*r._21+l.z*r._22\n+                                \"\"\")\n+                ).semicolon());\n","filename":"hat\/core\/src\/main\/java\/hat\/codebuilders\/C99VecAndMatHandler.java","additions":25,"deletions":9,"binary":false,"changes":34,"status":"modified"},{"patch":"@@ -35,0 +35,1 @@\n+\n@@ -50,0 +51,5 @@\n+import  hat.types.vec3;\n+import static hat.types.vec3.*;\n+import hat.types.F32;\n+import static hat.types.F32.*;\n+\n@@ -129,1 +135,1 @@\n-    public static void run(int bodyIdx, int bodies, @MappableIface.RW F32Array xyzPos, @MappableIface.RW F32Array xyzVel, @MappableIface.RW S32RGBAImage image, int imageWidth, float mass, float delT, float espSqr) {\n+    public static void runSansVec(int bodyIdx, int bodies, @MappableIface.RW F32Array xyzPos, @MappableIface.RW F32Array xyzVel, @MappableIface.RW S32RGBAImage image, int imageWidth, float mass, float delT, float espSqr) {\n@@ -172,0 +178,55 @@\n+        if (x > 1 && x < imageWidth - 2 && y > 1 && y < imageWidth - 2) {\n+            \/\/ Calculate brightness based on depth (Z)\n+            int brightness = (255 - (z \/ imageWidth * 255));\n+            int color = (brightness << 16) | (brightness << 8) | brightness;\n+            int pos = ((y * imageWidth) + x);\n+            image.data(pos, color);\n+            if (z < FAR) {\n+                image.data(pos + 1, color);\n+                image.data(pos - 1, color);\n+                image.data(pos + imageWidth, color);\n+                image.data(pos - imageWidth, color);\n+                if (z < MID) {\n+                    image.data(pos + imageWidth + 1, color);\n+                    image.data(pos + imageWidth - 1, color);\n+                    image.data(pos - imageWidth + 1, color);\n+                    image.data(pos - imageWidth - 1, color);\n+                    if (z < NEAR) {\n+                        image.data(pos + imageWidth * 2 + 2, color);\n+                        image.data(pos + imageWidth * 2 - 2, color);\n+                        image.data(pos - imageWidth * 2 + 2, color);\n+                        image.data(pos - imageWidth * 2 - 2, color);\n+                    }\n+                }\n+            }\n+        }\n+    }\n+\n+    @Reflect\n+    public static void run(int bodyIdx, int bodies, @MappableIface.RW F32Array xyzPos, @MappableIface.RW F32Array xyzVel, @MappableIface.RW S32RGBAImage image, int imageWidth, float mass, float delT, float espSqr) {\n+        final int FAR = 500;\n+        final int MID = 300;\n+        final int NEAR = 100;\n+        var acc = vec3(0f);\n+        var idx = bodyIdx * 4;\n+        var myPos = vec3(xyzPos.array(idx),xyzPos.array(idx+1),xyzPos.array(idx+2));\n+        var myVel = vec3(xyzVel.array(idx),xyzVel.array(idx+1),xyzVel.array(idx+2));\n+        for (int i = 0; i < bodies*4; i+=4) {\n+            var delta = sub(vec3(xyzPos.array(i),xyzPos.array(i+1),xyzPos.array(i+2)),myPos);\n+            float invDist = inversesqrt(dot(delta,delta)+espSqr);\n+            acc = vec3.add(acc,mul(delta,pow(invDist,3f)*mass));\n+        }\n+        acc = mul(acc,delT);\n+        var f = vec3.add(myPos,mul(vec3.add(myVel,mul(acc,.5f)),delT));\n+\n+        xyzPos.array(idx, f.x());\n+        xyzPos.array(idx+1, f.y());\n+        xyzPos.array(idx+2, f.z());\n+\n+        xyzVel.array(idx, myVel.x() + acc.x());\n+        xyzVel.array(idx+1, myVel.y() + acc.y());\n+        xyzVel.array(idx+2, myVel.z() + acc.z());\n+\n+        int x = (int) f.x();\n+        int y = (int) f.y();\n+        int z = (int) f.z();\n","filename":"hat\/examples\/nbody\/src\/main\/java\/nbody\/Main.java","additions":62,"deletions":1,"binary":false,"changes":63,"status":"modified"},{"patch":"@@ -76,1 +76,0 @@\n-import static hat.types.vec3.neg;\n@@ -303,4 +302,0 @@\n-    final  static public  vec3 SEA_BASE = vec3(0.0f, 0.09f, 0.18f);\n-    final  static public  vec3 SEA_WATER_COLOR = mul(vec3(0.8f, 0.9f, 0.6f), 0.6f);\n-\n-    final static mat2 octave_m = mat2(1.6f, 1.2f, -1.2f, 1.6f);\n@@ -545,0 +540,3 @@\n+        vec3 SEA_BASE = vec3(0.0f, 0.09f, 0.18f);\n+          vec3 SEA_WATER_COLOR = mul(vec3(0.8f, 0.9f, 0.6f), 0.6f);\n+\n@@ -546,1 +544,1 @@\n-        float fresnel = F32.clamp(1.0f - vec3.dot(n, neg(eye)), 0.0f, 1.0f);\n+        float fresnel = F32.clamp(1.0f - vec3.dot(n, mul(-1f,eye)), 0.0f, 1.0f);\n@@ -624,1 +622,2 @@\n-        for (int i = 0; i < NUM_STEPS; i++) {\n+        int brk=0;\n+        for (int i = 0; brk != 1 && i < NUM_STEPS; i++) {\n@@ -635,1 +634,1 @@\n-            if (abs(hmid) < EPSILON) break;\n+            if (abs(hmid) < EPSILON) brk=1;\n@@ -719,1 +718,1 @@\n-        final float EPSILON_NRM = 0.1f \/ fres.x();\n+        \/\/final float EPSILON_NRM = 0.1f \/ fres.x();\n@@ -722,1 +721,1 @@\n-        float time = ftime * 0.3f + fmouse.x() * 0.01f;\n+     \/\/   float time = ftime * 0.3f + fmouse.x() * 0.01f;\n@@ -774,1 +773,1 @@\n-        var shader = ShaderViewer.of(acc, SeaScapeShader.class,1024, 1024, false);\n+        var shader = ShaderViewer.of(acc, SeaScapeShader.class,1024, 1024, true);\n","filename":"hat\/examples\/shade\/src\/main\/java\/shade\/shaders\/SeaScapeShader.java","additions":10,"deletions":11,"binary":false,"changes":21,"status":"modified"},{"patch":"@@ -131,1 +131,1 @@\n-        var term = vec3.dot(normal,neg(ray_dir));\n+        var term = vec3.dot(normal,mul(-1f,ray_dir));\n@@ -188,1 +188,0 @@\n-        vec2 fres = vec2(uniforms.iResolution().x(),uniforms.iResolution().y());\n","filename":"hat\/examples\/shade\/src\/main\/java\/shade\/shaders\/Truchet2Shader.java","additions":1,"deletions":2,"binary":false,"changes":3,"status":"modified"},{"patch":"@@ -690,1 +690,1 @@\n-        var shader = ShaderViewer.of(acc, WavesShader.class,1024, 1024, false);\n+        var shader = ShaderViewer.of(acc, WavesShader.class,1024, 1024, true);\n","filename":"hat\/examples\/shade\/src\/main\/java\/shade\/shaders\/WavesShader.java","additions":1,"deletions":1,"binary":false,"changes":2,"status":"modified"},{"patch":"@@ -128,1 +128,3 @@\n-            case CoreOp.UnreachableOp $ -> throw new IllegalStateException(\"Found CoreOp.Unreachable\"+op);\n+            case CoreOp.UnreachableOp $ -> {;}\n+                \/\/throw new IllegalStateException(\"Found CoreOp.Unreachable\"+op);\n+           \/\/ }\n","filename":"hat\/optkl\/src\/main\/java\/optkl\/codebuilders\/BabylonOpDispatcher.java","additions":3,"deletions":1,"binary":false,"changes":4,"status":"modified"}]}