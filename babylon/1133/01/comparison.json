{"files":[{"patch":"@@ -2951,1 +2951,4 @@\n-            return lop instanceof StatementTargetOp.ResolvedStatementTarget || lop.target() == this || lop.target().isAncestorOf(this);\n+            Op target = lop.target();\n+            return target == this\n+                    || target.isAncestorOf(this)\n+                    || (lop instanceof StatementTargetOp.ResolvedStatementTarget && !isAncestorOf(target));\n@@ -5624,1 +5627,4 @@\n-            return lop instanceof StatementTargetOp.ResolvedStatementTarget || lop.target() == this || lop.target().isAncestorOf(this);\n+            Op target = lop.target();\n+            return target == this\n+                    || target.isAncestorOf(this)\n+                    || (lop instanceof StatementTargetOp.ResolvedStatementTarget && !isAncestorOf(target));\n","filename":"src\/jdk.incubator.code\/share\/classes\/jdk\/incubator\/code\/dialect\/java\/JavaOp.java","additions":8,"deletions":2,"binary":false,"changes":10,"status":"modified"},{"patch":"@@ -634,0 +634,30 @@\n+    @Reflect\n+    static int continueFromTryWhileTWR(int value) {\n+        int i = 0;\n+        try {\n+            while (i++ < value) {\n+                try (var resource = Stream.empty()) {\n+                    for (int ignored : new int[] { 1 }) {\n+                    }\n+                    continue;\n+                }\n+            }\n+        } finally {\n+            i++;\n+        }\n+        return i;\n+    }\n+\n+    @Reflect\n+    static int continueFromSynchronizedWhileTWR(int value) {\n+        int i = 0;\n+        synchronized (TestBytecode.class) {\n+            while (i++ < value) {\n+                try (var resource = Stream.empty()) {\n+                    continue;\n+                }\n+            }\n+        }\n+        return i;\n+    }\n+\n","filename":"test\/jdk\/jdk\/incubator\/code\/bytecode\/TestBytecode.java","additions":30,"deletions":0,"binary":false,"changes":30,"status":"modified"}]}