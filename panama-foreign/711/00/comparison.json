{"files":[{"patch":"@@ -360,5 +360,12 @@\n-\n-            return VectorSupport.load(vmClass, e, length,\n-                    msp.unsafeGetBase(), msp.unsafeGetOffset() + offset,\n-                    msp, offset, s,\n-                    defaultImpl);\n+            Object base = msp.unsafeGetBase();\n+            if (base == null) {\n+                return VectorSupport.load(vmClass, e, length,\n+                        null, msp.unsafeGetOffset() + offset,\n+                        msp, offset, s,\n+                        defaultImpl);\n+            } else {\n+                return VectorSupport.load(vmClass, e, length,\n+                                        msp.unsafeGetBase(), msp.unsafeGetOffset() + offset,\n+                                        msp, offset, s,\n+                                        defaultImpl);\n+            }\n@@ -451,6 +458,14 @@\n-\n-            VectorSupport.store(vmClass, e, length,\n-                    msp.unsafeGetBase(), msp.unsafeGetOffset() + offset,\n-                    v,\n-                    msp, offset,\n-                    defaultImpl);\n+            Object base = msp.unsafeGetBase();\n+            if (base == null) {\n+                VectorSupport.store(vmClass, e, length,\n+                        null, msp.unsafeGetOffset() + offset,\n+                        v,\n+                        msp, offset,\n+                        defaultImpl);\n+            } else {\n+                VectorSupport.store(vmClass, e, length,\n+                                    msp.unsafeGetBase(), msp.unsafeGetOffset() + offset,\n+                                    v,\n+                                    msp, offset,\n+                                    defaultImpl);\n+            }\n","filename":"src\/java.base\/share\/classes\/jdk\/internal\/misc\/X-ScopedMemoryAccess.java.template","additions":26,"deletions":11,"binary":false,"changes":37,"status":"modified"}]}